<!--
    A PREVIEW PAGE, NOT A PRODUCT ONE. DESIGN 11.2's real pages live on the
    SHARE under Scripts\UI so they can be redesigned without rebuilding a boot
    image; these three exist only so the SHELL can be looked at - the rail, the
    heading, the swap and the buttons - before any of them are designed.

    xmlns AND xmlns:x ARE BOTH DECLARED. A page fragment is loaded standalone by
    XamlReader, not as part of the window, so it carries no namespace by
    inheritance - and one using x:Name without xmlns:x throws "'x' is an
    undeclared prefix" at load, which on a bench looks like a wizard that failed
    to open.
-->
<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Task sequence" Foreground="#FFCCCCCC" FontSize="14" Margin="0,0,0,8" />

    <!--
        SelectedValuePath="Tag" IS WHAT MAKES THIS COLLECTABLE. The page
        declares that HDTTaskSequenceID is read from SelectedValue, and the host
        reads that named property and nothing else - so a ListBox and a TextBox
        are the same thing to it. Without a value path, SelectedValue would be
        the ListBoxItem, and the summary would show a control's type name where
        a sequence ID belongs.
    -->
    <ListBox Grid.Row="1" x:Name="HDTTaskSequenceList" SelectedValuePath="Tag"
             Background="#FF252526" Foreground="White" BorderBrush="#FF3A3A3C"
             FontSize="15" Padding="4">
        <ListBoxItem Tag="STD-CLIENT" Content="STD-CLIENT  -  Standard Windows 11 client" IsSelected="True" />
        <ListBoxItem Tag="DEMO-M4" Content="DEMO-M4  -  Five-step demonstration" />
        <ListBoxItem Tag="WS2025-STD" Content="WS2025-STD  -  Windows Server 2025, Desktop Experience" />
    </ListBox>
</Grid>
