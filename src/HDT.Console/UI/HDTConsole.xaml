<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Hephaestus Deployment Toolkit"
    Height="720" Width="1180"
    MinHeight="520" MinWidth="900"
    WindowStartupLocation="CenterScreen"
    Background="#FF1E1E1E">

    <!--
        C1 of the WPF-first direction: the admin console, showing what is on a
        deployment share.

        NO x:Class AND NO CODE-BEHIND, exactly as HDTWizard.xaml. The console
        runs on a desktop where a compiler exists, so this one is a choice rather
        than a constraint - and the choice is that a page written this way can
        move into the WinPE wizard later without being rewritten, where
        XamlReader::Load is the only option there is. Handlers are attached in
        PowerShell, by name, after the tree is loaded.

        NAMED CONTROLS ARE THE CONTRACT between this file and New-HDTConsoleHost:
        HDTShareText, HDTDeployRootText, HDTRootText, HDTConsoleList,
        HDTDetailText, HDTCommandText and HDTCloseButton are found with FindName,
        and tests/unit/ConsoleWindow.Tests.ps1 asserts every one of those names is
        present, so renaming one here fails on a developer machine rather than in
        front of an administrator.

        THE LIST IS FLAT AND MONOSPACED, NOT A TreeView. Get-HDTConsoleTreeNode
        emits Display with its indentation already applied, so the tree shape is
        a string this window merely renders - and a string is something a unit
        test can assert. A TreeView would have to be built by the host, which is
        the one component with no tests. The monospaced font is not decoration
        either: this window shows 64-character SHA-256 hashes, and hashes are
        read by comparing columns.

        DisplayMemberPath BINDS TO THE Display MEMBER of the row objects the host
        is handed. Nothing else in this file binds to anything, so there is no
        DataContext, no converter and no MVVM plumbing to keep in step with the
        commands.
    -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="64" />
        </Grid.RowDefinitions>

        <!-- banner: the share, and BOTH of its paths -->
        <Border Grid.Row="0" Background="#FF0E639C" Padding="24,16,24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                           x:Name="HDTShareText"
                           Text="Deployment share"
                           Foreground="White" FontSize="21" FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis" />

                <TextBlock Grid.Row="1" Grid.Column="0"
                           Text="Deploy root" Foreground="#FFBEDCF0"
                           FontSize="12" Margin="0,10,14,0" Width="86" />
                <TextBlock Grid.Row="1" Grid.Column="1"
                           x:Name="HDTDeployRootText"
                           Text="" Foreground="White" FontSize="12"
                           FontFamily="Consolas, Courier New" Margin="0,10,0,0"
                           TextTrimming="CharacterEllipsis" />

                <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="Opened from" Foreground="#FFBEDCF0"
                           FontSize="12" Margin="0,4,14,0" Width="86" />
                <TextBlock Grid.Row="2" Grid.Column="1"
                           x:Name="HDTRootText"
                           Text="" Foreground="White" FontSize="12"
                           FontFamily="Consolas, Courier New" Margin="0,4,0,0"
                           TextTrimming="CharacterEllipsis" />
            </Grid>
        </Border>

        <!-- body: the tree on the left, the selected row on the right -->
        <Grid Grid.Row="1" Margin="24,20,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Deployment share"
                       Foreground="#FF9CDCFE" FontSize="12" Margin="0,0,0,6" />

            <ListBox Grid.Row="1" Grid.Column="0"
                     x:Name="HDTConsoleList"
                     DisplayMemberPath="Display"
                     Background="#FF252526" Foreground="#FFE6E6E6"
                     BorderBrush="#FF3C3C3C" BorderThickness="1"
                     FontFamily="Consolas, Courier New" FontSize="13"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" />

            <TextBlock Grid.Row="0" Grid.Column="2" Text="Details"
                       Foreground="#FF9CDCFE" FontSize="12" Margin="0,0,0,6" />

            <TextBox Grid.Row="1" Grid.Column="2"
                     x:Name="HDTDetailText"
                     Text=""
                     IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                     Background="#FF252526" Foreground="#FFE6E6E6"
                     BorderBrush="#FF3C3C3C" BorderThickness="1"
                     FontFamily="Consolas, Courier New" FontSize="13"
                     Padding="12"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" />

            <!--
                DESIGN 12: "the console shows that invocation - so an admin can
                learn the automation surface by clicking around, and script
                anything they can do in the UI". Selectable, so it can be copied.
            -->
            <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Command"
                           Foreground="#FF9CDCFE" FontSize="12"
                           VerticalAlignment="Center" Margin="0,0,12,0" />

                <TextBox Grid.Column="1"
                         x:Name="HDTCommandText"
                         Text=""
                         IsReadOnly="True" TextWrapping="NoWrap"
                         Background="#FF1B1B1B" Foreground="#FFCE9178"
                         BorderBrush="#FF3C3C3C" BorderThickness="1"
                         FontFamily="Consolas, Courier New" FontSize="13"
                         Padding="8,6,8,6"
                         HorizontalScrollBarVisibility="Auto" />
            </Grid>
        </Grid>

        <!-- footer -->
        <Border Grid.Row="2" Background="#FF252526" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="0,0,24,0">
                <Button x:Name="HDTCloseButton" Content="Close"
                        Width="112" Height="34"
                        Background="#FF0E639C" Foreground="White" BorderThickness="0"
                        IsDefault="True" IsCancel="True" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
