# A setFrom: script that emits nothing, so "the script produced no output" is a
# real script on the real IScriptInvoker row rather than a fake-only seed.
param([System.Collections.IDictionary] $Variable)
