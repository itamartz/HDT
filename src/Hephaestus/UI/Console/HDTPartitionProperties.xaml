<!--
    MDT'S "Partition Properties" DIALOG - the modal its Format and Partition
    Disk page opens for New and for Edit.

    IT IS A MODAL AND NOT A ROW OF BOXES UNDER THE GRID. A volume has eight
    things to say about it and two of them are checkboxes; laid out along the
    bottom of a page they read as filters over the list rather than as the
    thing being edited, and there is nowhere honest to put OK and Cancel.

    NOTHING HERE DECIDES ANYTHING. The unit list, what each unit composes, and
    what the boxes start out holding all come from Get-HDTConsolePartitionRow;
    this window is the shape they are shown in, and OK hands them back to
    Add-HDTStepPartition or Set-HDTStepPartition.
-->
<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Partition Properties"
        Width="470" Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource HDTPanelBrush}"
        FontFamily="Segoe UI" FontSize="12">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource HDTPanelTextBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>

        <Style TargetType="TextBox">
            <!-- WHITE, BECAUSE THESE ARE TYPED IN. HDTFieldBrush is the editor's
                 read-only wash, and using it here would say the opposite. -->
            <Setter Property="Background" Value="{DynamicResource HDTPanelBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource HDTPanelTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource HDTBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,5,6,5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource HDTPanelTextBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="88" />
            <Setter Property="Padding" Value="10,5,10,5" />
            <Setter Property="Margin" Value="8,0,0,0" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Volume name" />
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="HDTVolumeNameBox" Margin="0,0,0,10"
                 ToolTip="What the volume is called - System, Windows, Recovery." />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Partition type" />
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="HDTVolumeTypeBox" Margin="0,0,0,10"
                  ToolTip="A word, never a GUID. Logical and Extended are refused: this engine creates basic partitions.">
            <ComboBoxItem Content="Primary" IsSelected="True" />
            <ComboBoxItem Content="EFI" />
            <ComboBoxItem Content="Recovery" />
        </ComboBox>

        <!--
            MDT PUTS THE PERCENTAGE BEHIND A RADIO PAIR and then has the units
            dropdown quietly ignore it. Here a percentage is one of the units,
            which is the same choice said once - and "the rest of the disk" is
            on the same list because it is the same question.
        -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Size" />
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" x:Name="HDTVolumeSizeBox" Margin="0,0,8,0"
                     ToolTip="How much - 260, 1, 60. What of is the list beside it." />

            <ComboBox Grid.Column="1" x:Name="HDTVolumeUnitBox" DisplayMemberPath="Display"
                      ToolTip="MB, GB, TB, a percentage of what is left, raw bytes, or all of the rest." />
        </Grid>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="File system" />
        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="HDTVolumeFileSystemBox" Margin="0,0,0,10"
                  ToolTip="Left as the engine's default, the type decides - FAT32 for an ESP, NTFS otherwise.">
            <ComboBoxItem Content="(default)" IsSelected="True" />
            <ComboBoxItem Content="NTFS" />
            <ComboBoxItem Content="FAT32" />
        </ComboBox>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Variable" />
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="HDTVolumeVariableBox" Margin="0,0,0,14"
                 ToolTip="A variable to publish this volume's drive letter into - MDT's VolumeLetterVariable. The engine's own are HDTSystemVolume, HDTOSVolume and HDTRecoveryVolume." />

        <CheckBox Grid.Row="5" Grid.Column="1" x:Name="HDTVolumeQuickFormatCheck"
                  Content="Quick format" Margin="0,0,0,8"
                  ToolTip="Unticked writes quickFormat: false. Ticked is the engine's default and writes nothing." />

        <CheckBox Grid.Row="6" Grid.Column="1" x:Name="HDTVolumeBootableCheck"
                  Content="Make this a boot partition"
                  ToolTip="One partition at most. The engine makes the first one bootable when no row claims it." />

        <!-- WHAT THE REFUSAL SAYS, WHERE IT WAS CAUSED. OK runs the command;
             if the command refuses - a size the engine cannot read, a second
             boot partition - the message lands here rather than in a message
             box over a dialog that has already closed. -->
        <TextBlock Grid.Row="7" Grid.ColumnSpan="2" x:Name="HDTVolumeMessageText"
                   Foreground="{DynamicResource HDTErrorBrush}"
                   TextWrapping="Wrap" VerticalAlignment="Bottom" Margin="0,12,0,0" />

        <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="HDTVolumeOkButton" Content="OK" IsDefault="True" />
            <!-- NO NAME, BECAUSE NOTHING WIRES IT. IsCancel is WPF closing
                 its own dialog with DialogResult false; a name here would
                 promise a handler that does not exist, which is exactly what
                 the contract test looks for. -->
            <Button Content="Cancel" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
