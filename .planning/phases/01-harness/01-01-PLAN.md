---
phase: 01-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Hephaestus/Hephaestus.psd1
  - src/Hephaestus/Hephaestus.psm1
  - src/Hephaestus/Public/Get-HDTModuleVersion.ps1
  - src/Hephaestus/Private/Test-HDTSchemaVersion.ps1
  - tests/helpers/HDTTestTools/HDTTestTools.psd1
  - tests/helpers/HDTTestTools/HDTTestTools.psm1
  - tests/helpers/HDTTestTools/tools/Get-HDTSourceFile.ps1
  - tests/helpers/HDTTestTools/tools/New-HDTPesterConfiguration.ps1
  - tests/unit/Module.Manifest.Tests.ps1
  - tests/unit/Module.Loader.Tests.ps1
  - tests/unit/Get-HDTModuleVersion.Tests.ps1
  - tests/unit/Test-HDTSchemaVersion.Tests.ps1
  - tests/unit/Get-HDTSourceFile.Tests.ps1
  - tests/unit/New-HDTPesterConfiguration.Tests.ps1
  - build.ps1
  - PSScriptAnalyzerSettings.psd1
  - README.md
  - Readme.txt
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "A developer can run ./build.ps1 test and the whole unit suite runs and reports pass/fail"
    - "The same command works under pwsh 7 and under powershell.exe 5.1"
    - "Importing the module exports exactly the public functions and no private ones"
    - "./build.ps1 lint reports zero PSScriptAnalyzer diagnostics on the repo's own PowerShell files"
    - "A failing test makes build.ps1 exit non-zero"
  artifacts:
    - path: "src/Hephaestus/Hephaestus.psd1"
      provides: "Module manifest, PowerShellVersion 5.1, explicit FunctionsToExport"
      contains: "FunctionsToExport"
    - path: "src/Hephaestus/Hephaestus.psm1"
      provides: "Dot-sourcing loader with Set-StrictMode and $ErrorActionPreference='Stop'"
      contains: "Export-ModuleMember"
    - path: "build.ps1"
      provides: "build / test / lint / ci task runner with correct exit codes"
      contains: "ValidateSet"
    - path: "PSScriptAnalyzerSettings.psd1"
      provides: "Analyzer configuration incl. PSUseCompatibleSyntax for 5.1 and 7.0"
      contains: "PSUseCompatibleSyntax"
    - path: "tests/helpers/HDTTestTools/HDTTestTools.psm1"
      provides: "Shared build+test helper module"
      exports: ["Get-HDTSourceFile", "New-HDTPesterConfiguration"]
  key_links:
    - from: "build.ps1"
      to: "tests/helpers/HDTTestTools/HDTTestTools.psd1"
      via: "Import-Module by path in Initialize-HDTBuildEnvironment"
      pattern: "HDTTestTools"
    - from: "build.ps1"
      to: "Pester 5"
      via: "Import-Module Pester -MinimumVersion 5.0.0 -MaximumVersion 5.99.99"
      pattern: "MaximumVersion"
    - from: "src/Hephaestus/Hephaestus.psm1"
      to: "src/Hephaestus/Public/*.ps1"
      via: "dot-source + Export-ModuleMember"
      pattern: "Export-ModuleMember"
---

<objective>
Stand up the Hephaestus module skeleton and the build/test harness that every
later plan and phase runs through.

Purpose: nothing in HDT can be written test-first until there is a green,
repeatable way to run tests under **both** pwsh 7 and Windows PowerShell 5.1.
This plan builds that runway and proves it with real tests over real (small)
production code.

Output: `src/Hephaestus/` module (manifest + loader + one public + one private
function), `tests/helpers/HDTTestTools/` shared helper module, `build.ps1` with
`build`/`test`/`lint`/`ci` tasks, `PSScriptAnalyzerSettings.psd1`, `README.md`.
</objective>

<execution_context>
@C:/Users/Itamartz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Itamartz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/DESIGN.md
@docs/ROADMAP.md

Sections that bind this plan: DESIGN §12.2 (TDD), §12.2.5 (CI), §15.1 (naming),
§15.2 (conventions), §12.3 (schemaVersion refusal), §1 "Explicit constraint:
PowerShell 5.1". ROADMAP M0 "Tests first" bullets.
</context>

<environment_facts>
Verified on this machine before planning — do not re-derive, but do re-check if
something fails:

| Fact | Value | Consequence |
|---|---|---|
| pwsh | 7.5.8 | `pwsh -NoProfile` |
| Windows PowerShell | 5.1.26100.8655 | `powershell.exe -NoProfile` |
| Pester visible to 5.1 | **6.0.0, 5.7.1, 3.4.0** | A bare `Import-Module Pester` under 5.1 loads **6.0.0**. Pester import MUST be pinned to `-MinimumVersion 5.0.0 -MaximumVersion 5.99.99`. This is not optional. |
| Pester visible to pwsh 7 | 5.7.1, 3.4.0 | same pinned import works |
| PSScriptAnalyzer under pwsh 7 | 1.25.0, importable | `lint` works under pwsh 7 |
| PSScriptAnalyzer under 5.1 | **not importable** (only inside an unrelated module's RequiredModules tree) | `lint` must fail with an actionable message under 5.1, and `test` must NOT depend on `lint`. The exit criterion is `./build.ps1 test`, which must be PSSA-free. |
| Pester 5 config API under 5.1 | `New-PesterConfiguration`, `$c.Run/*`, `$c.Filter.ExcludeTag`, NUnitXml output all work | safe to use |
| PSUseCompatibleSyntax with `TargetVersions = @('5.1','7.0')` | flags `??` as **Error** | second layer of 5.1 enforcement, enable it |
</environment_facts>

<tasks>

<task type="auto">
  <name>Task 1: Module skeleton, RED then GREEN</name>
  <files>
tests/unit/Module.Manifest.Tests.ps1
tests/unit/Module.Loader.Tests.ps1
tests/unit/Get-HDTModuleVersion.Tests.ps1
tests/unit/Test-HDTSchemaVersion.Tests.ps1
src/Hephaestus/Hephaestus.psd1
src/Hephaestus/Hephaestus.psm1
src/Hephaestus/Public/Get-HDTModuleVersion.ps1
src/Hephaestus/Private/Test-HDTSchemaVersion.ps1
  </files>
  <action>
**RED first. Write all four test files, run them, and confirm they fail for the
right reason (module does not exist) before creating anything under
`src/Hephaestus/`.**

Run tests during this task with:
```
pwsh -NoProfile -Command "Import-Module Pester -MinimumVersion 5.0.0 -MaximumVersion 5.99.99 -Force; Invoke-Pester -Path ./tests/unit -Output Detailed"
```
and the same with `powershell.exe`. Commit the RED state first
(`test(01-01): failing tests for module skeleton`), then the GREEN state
(`feat(01-01): module manifest, loader and first functions`).

Every test file starts with a `BeforeAll` that resolves the repo root from
`$PSScriptRoot` (`Split-Path -Parent (Split-Path -Parent $PSScriptRoot)`) and
imports the module by **manifest path** with `-Force`.

---

### tests/unit/Module.Manifest.Tests.ps1

`Describe 'Hephaestus module manifest'`
- `It 'exists at src/Hephaestus/Hephaestus.psd1'`
- `It 'passes Test-ModuleManifest'` — `{ Test-ModuleManifest -Path $manifestPath } | Should -Not -Throw`
- `It 'declares RootModule Hephaestus.psm1'`
- `It 'requires PowerShell 5.1'` — `PowerShellVersion` -eq `'5.1'`
- `It 'declares both PS editions'` — `CompatiblePSEditions` contains `Desktop` and `Core`
- `It 'exports functions explicitly, never a wildcard'` — read the raw manifest
  with `Import-PowerShellDataFile`; `FunctionsToExport` is an array and
  `-notcontains '*'`
- `It 'exports no cmdlets, aliases or variables'` — those three keys are empty arrays
- `It 'lists exactly one exported function per file in Public'` — compare
  `FunctionsToExport` (sorted) to `Get-ChildItem Public -Filter *.ps1 | % BaseName` (sorted)
- `It 'has a valid GUID'` — `[guid]::Parse($data.GUID)` does not throw

### tests/unit/Module.Loader.Tests.ps1

`Describe 'Hephaestus module loader'`
- `It 'imports without error'` — `{ Import-Module $manifestPath -Force -ErrorAction Stop } | Should -Not -Throw`
- `It 'exports exactly the functions named in the manifest'` — compare
  `(Get-Module Hephaestus).ExportedFunctions.Keys` sorted to manifest `FunctionsToExport` sorted
- `It 'does not export the private function Test-HDTSchemaVersion'` —
  `Get-Command Test-HDTSchemaVersion -ErrorAction SilentlyContinue | Should -BeNullOrEmpty`
- `It 'makes private functions callable inside module scope'` —
  `InModuleScope Hephaestus { Test-HDTSchemaVersion -SchemaVersion 1 -Supported 1 } | Should -BeTrue`
- `It 'sets StrictMode Latest in the module file'` — the raw text of
  `Hephaestus.psm1` matches `Set-StrictMode -Version Latest`
- `It 'sets $ErrorActionPreference to Stop in the module file'`
- `It 'survives a second -Force import'` — import twice, assert export count unchanged
- `It 'has a dot-source loader that tolerates an empty Public or Private folder'` —
  the loader uses `-ErrorAction SilentlyContinue` on `Get-ChildItem` (assert by
  running the loader against a temp copy of the module with `Private` removed;
  `Import-Module` must still succeed)

### tests/unit/Get-HDTModuleVersion.Tests.ps1

`Describe 'Get-HDTModuleVersion'`
- `It 'returns a [version] object'`
- `It 'returns the version declared in the manifest'` — compare to
  `(Import-PowerShellDataFile $manifestPath).ModuleVersion`
- `It 'takes no mandatory parameters'` — `(Get-Command Get-HDTModuleVersion).Parameters` has no mandatory non-common parameter
- `It 'has comment-based help with a synopsis'` — `(Get-Help Get-HDTModuleVersion).Synopsis` is not empty and is not the function name

### tests/unit/Test-HDTSchemaVersion.Tests.ps1

Run these through `InModuleScope Hephaestus { ... }` because the function is private.
Behaviour per DESIGN §12.3 — "the module refuses to operate on a workspace newer
than it understands".

`Describe 'Test-HDTSchemaVersion'`
- `It 'accepts a schemaVersion equal to the supported version'` — 1,1 -> `$true`
- `It 'accepts an older schemaVersion'` — 1,2 -> `$true`
- `It 'rejects a schemaVersion newer than supported'` — 3,2 -> `$false`
- `It 'throws on a schemaVersion below 1'` — `{ ... -SchemaVersion 0 -Supported 1 }` throws, message matches `schemaVersion`
- `It 'throws when SchemaVersion is not supplied'` — parameter is mandatory
- `It 'returns a plain boolean, not a truthy object'` — result `-is [bool]`

---

### Implementation (only after the above are RED)

**src/Hephaestus/Hephaestus.psd1** — generate the GUID with `[guid]::NewGuid()`
and paste the literal value in (do not generate it at import time). Fields:
`RootModule='Hephaestus.psm1'`, `ModuleVersion='0.1.0'`, `GUID=<generated>`,
`Author='Itamartz'`, `CompanyName='Hephaestus Deployment Toolkit'`,
`Copyright='(c) 2026 Itamartz. All rights reserved.'`,
`Description='Hephaestus Deployment Toolkit - a PowerShell replacement for MDT.'`,
`PowerShellVersion='5.1'`, `CompatiblePSEditions=@('Desktop','Core')`,
`FunctionsToExport=@('Get-HDTModuleVersion')`, `CmdletsToExport=@()`,
`VariablesToExport=@()`, `AliasesToExport=@()`,
`PrivateData=@{ PSData=@{ Tags=@('MDT','Deployment','OSD','WinPE') } }`.
No `DefaultCommandPrefix` — DESIGN §15.1 forbids it explicitly.

**src/Hephaestus/Hephaestus.psm1** — 5.1-safe loader:
```powershell
Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

$script:HDTModuleRoot = $PSScriptRoot

$privateFile = @(Get-ChildItem -Path (Join-Path -Path $PSScriptRoot -ChildPath 'Private') -Filter '*.ps1' -Recurse -ErrorAction SilentlyContinue)
$publicFile  = @(Get-ChildItem -Path (Join-Path -Path $PSScriptRoot -ChildPath 'Public')  -Filter '*.ps1' -Recurse -ErrorAction SilentlyContinue)

foreach ($file in @($privateFile + $publicFile)) {
    try {
        . $file.FullName
    } catch {
        throw ("Failed to dot-source '{0}': {1}" -f $file.FullName, $_.Exception.Message)
    }
}

Export-ModuleMember -Function ($publicFile | ForEach-Object { $_.BaseName })
```
(Use `| ForEach-Object { $_.BaseName }`, not `$publicFile.BaseName` — member
enumeration over an empty array under `Set-StrictMode -Version Latest` is the
kind of thing that behaves differently between engines.)

**src/Hephaestus/Public/Get-HDTModuleVersion.ps1** — comment-based help
(.SYNOPSIS/.DESCRIPTION/.OUTPUTS/.EXAMPLE), `[CmdletBinding()]`,
`[OutputType([version])]`, returns `$MyInvocation.MyCommand.Module.Version`.
Justified by DESIGN §12.3 (boot images record the engine version they contain).

**src/Hephaestus/Private/Test-HDTSchemaVersion.ps1** —
```
Test-HDTSchemaVersion [-SchemaVersion] <int> [-Supported] <int> -> [bool]
```
`[CmdletBinding()]`, `[OutputType([bool])]`, both parameters `[Parameter(Mandatory)]`,
`[ValidateRange(1, 2147483647)]` on `SchemaVersion` is NOT enough for the
"throws with a message mentioning schemaVersion" test — throw explicitly:
`throw "Unsupported schemaVersion '$SchemaVersion': schemaVersion must be 1 or greater."`
Then `return ($SchemaVersion -le $Supported)`.
  </action>
  <verify>
```
pwsh -NoProfile -Command "Import-Module Pester -MinimumVersion 5.0.0 -MaximumVersion 5.99.99 -Force; \$r = Invoke-Pester -Path ./tests/unit -PassThru; exit \$r.FailedCount"
powershell.exe -NoProfile -Command "Import-Module Pester -MinimumVersion 5.0.0 -MaximumVersion 5.99.99 -Force; \$r = Invoke-Pester -Path ./tests/unit -PassThru; exit \$r.FailedCount"
```
Both exit 0. `git log --oneline` shows a RED commit before the GREEN commit.
  </verify>
  <done>
All four test files exist, all Its pass under both engines, `Get-HDTModuleVersion`
is exported, `Test-HDTSchemaVersion` is not, and the RED-before-GREEN commit
order is visible in git history.
  </done>
</task>

<task type="auto">
  <name>Task 2: HDTTestTools helper module, RED then GREEN</name>
  <files>
tests/unit/Get-HDTSourceFile.Tests.ps1
tests/unit/New-HDTPesterConfiguration.Tests.ps1
tests/helpers/HDTTestTools/HDTTestTools.psd1
tests/helpers/HDTTestTools/HDTTestTools.psm1
tests/helpers/HDTTestTools/tools/Get-HDTSourceFile.ps1
tests/helpers/HDTTestTools/tools/New-HDTPesterConfiguration.ps1
  </files>
  <action>
This module is shared by `build.ps1` and by the contract tests in plan 01-02.
Its functions obey the same `Verb-HDTNoun` rule as everything else (DESIGN §15.1
applies to test helpers and build functions explicitly).

**RED first: write both test files and watch them fail.**

### tests/unit/Get-HDTSourceFile.Tests.ps1

`Get-HDTSourceFile` enumerates every PowerShell **source** file in the repo that
is subject to HDT's own rules (naming, 5.1 compatibility, analyzer).

Signature: `Get-HDTSourceFile [-RepositoryRoot] <string> -> [string[]]` (full paths, sorted).

Include: `*.ps1` and `*.psm1` under `src/`, under `tests/`, plus `build.ps1` at
the root. Exclude: `tests/fixtures/**` (fixtures are deliberately wrong on
purpose), `out/**`, `.git/**`, `bin/**`, `obj/**`. `*.psd1` is never returned —
manifests define no functions and are not parsed for syntax.

`Describe 'Get-HDTSourceFile'` (build the inputs as a temp tree under
`$TestDrive` so the assertions are exact, then add one It against the real repo):
- `It 'returns .ps1 files under src'`
- `It 'returns .psm1 files under src'`
- `It 'returns build.ps1 from the repository root'`
- `It 'returns .ps1 files under tests'`
- `It 'excludes anything under tests/fixtures'`
- `It 'excludes .psd1 manifests'`
- `It 'excludes the out directory'`
- `It 'returns absolute paths'`
- `It 'returns a sorted, duplicate-free list'`
- `It 'throws when RepositoryRoot does not exist'`
- `It 'finds at least one file in the real repository'` — called with the real repo root

### tests/unit/New-HDTPesterConfiguration.Tests.ps1

Signature:
```
New-HDTPesterConfiguration [-Path] <string[]> [-ResultPath <string>] [-ExcludeTag <string[]>] [-Verbosity <string>] -> [PesterConfiguration]
```
`BeforeAll` imports Pester pinned to 5.x, then the helper module.

`Describe 'New-HDTPesterConfiguration'`
- `It 'returns a PesterConfiguration object'` — type name is `PesterConfiguration`
- `It 'sets Run.Path to the supplied paths'`
- `It 'enables PassThru so the caller can inspect the result'`
- `It 'leaves Run.Exit disabled so build.ps1 controls the exit code'` — `Run.Exit.Value` is `$false`
- `It 'enables NUnitXml test results at the supplied ResultPath'` — `TestResult.Enabled` true, `TestResult.OutputFormat.Value -eq 'NUnitXml'`, `TestResult.OutputPath.Value` matches
- `It 'disables test results when no ResultPath is supplied'`
- `It 'applies ExcludeTag to Filter.ExcludeTag'`
- `It 'defaults Output.Verbosity to Detailed'`
- `It 'honours an explicit Verbosity'`
- `It 'sets Should.ErrorAction to Stop'`
- `It 'throws when Path is empty'`

### Implementation

`tests/helpers/HDTTestTools/HDTTestTools.psm1` uses the same dot-source loader
shape as the engine module (`tools/*.ps1`) and exports both function names
explicitly. `HDTTestTools.psd1`: `RootModule='HDTTestTools.psm1'`,
`ModuleVersion='0.1.0'`, generated GUID, `PowerShellVersion='5.1'`,
`CompatiblePSEditions=@('Desktop','Core')`,
`FunctionsToExport=@('Get-HDTSourceFile','New-HDTPesterConfiguration')`, empty
Cmdlets/Variables/Aliases arrays.

`Get-HDTSourceFile` — implement with a single `Get-ChildItem -Recurse -File`
over the include roots plus a `Where-Object` exclusion filter built from
normalised paths. Do not use `-Exclude` (it matches leaf names only). Normalise
with `[System.IO.Path]::GetFullPath()` and compare case-insensitively.

`New-HDTPesterConfiguration` — build via `New-PesterConfiguration`; throw a clear
error if `New-PesterConfiguration` is unavailable ("Pester 5 is required; import
it with -MinimumVersion 5.0.0 -MaximumVersion 5.99.99").

Both functions get comment-based help. No PS7-only syntax anywhere.
  </action>
  <verify>
```
pwsh -NoProfile -Command "Import-Module Pester -MinimumVersion 5.0.0 -MaximumVersion 5.99.99 -Force; \$r = Invoke-Pester -Path ./tests/unit -PassThru; exit \$r.FailedCount"
powershell.exe -NoProfile -Command "Import-Module Pester -MinimumVersion 5.0.0 -MaximumVersion 5.99.99 -Force; \$r = Invoke-Pester -Path ./tests/unit -PassThru; exit \$r.FailedCount"
```
Both exit 0.
  </verify>
  <done>
`Import-Module ./tests/helpers/HDTTestTools/HDTTestTools.psd1` exports exactly
`Get-HDTSourceFile` and `New-HDTPesterConfiguration`; both are covered by passing
tests under both engines; RED commit precedes GREEN commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: build.ps1, analyzer settings, README</name>
  <files>
build.ps1
PSScriptAnalyzerSettings.psd1
README.md
Readme.txt
.gitignore
  </files>
  <action>
### build.ps1

```powershell
[CmdletBinding()]
param(
    [ValidateSet('clean','build','lint','test','ci')]
    [string[]] $Task = @('test'),
    [string] $Verbosity = 'Detailed'
)
```
`Set-StrictMode -Version Latest`, `$ErrorActionPreference = 'Stop'` at the top.

Functions inside build.ps1 — **all `Verb-HDTNoun`, approved verbs only**
(`Assert` and `Ensure` are NOT approved verbs; do not use them):

| Function | Behaviour |
|---|---|
| `Initialize-HDTBuildEnvironment` | Resolves `$script:HDTRepositoryRoot = $PSScriptRoot` and `$script:HDTOutputPath = Join-Path $PSScriptRoot 'out'`. Imports Pester with `-MinimumVersion 5.0.0 -MaximumVersion 5.99.99` (mandatory — 5.1 sees Pester 6.0.0 first). Imports `tests/helpers/HDTTestTools/HDTTestTools.psd1 -Force`. Throws an actionable message naming the exact `Install-Module` command if Pester 5 is missing. |
| `Clear-HDTBuildOutput` | Removes `out/` if present. |
| `Invoke-HDTBuild` | `Test-ModuleManifest` on the engine manifest; `Import-Module -Force`; assert exported functions match `FunctionsToExport`; copy `src/Hephaestus` to `out/Hephaestus/<ModuleVersion>/`. Throws on any mismatch. |
| `Invoke-HDTLint` | Requires PSScriptAnalyzer — if `Get-Module -ListAvailable PSScriptAnalyzer` is empty, throw: `"PSScriptAnalyzer is not available to this PowerShell edition. Run: Install-Module PSScriptAnalyzer -Scope CurrentUser"`. Runs `Invoke-ScriptAnalyzer -Path <each file from Get-HDTSourceFile> -Settings ./PSScriptAnalyzerSettings.psd1`. Also analyses `src/Hephaestus/Hephaestus.psd1`. Writes every diagnostic as `<Severity> <RuleName> <File>:<Line> <Message>` and throws if any diagnostic was produced. |
| `Invoke-HDTTest` | `New-HDTPesterConfiguration -Path @('tests/unit','tests/contract') -ResultPath 'out/testResults/pester-<edition>-<psversion>.xml' -Verbosity $Verbosity`; `Invoke-Pester -Configuration $config`; throws if `FailedCount -gt 0`. **`tests/fixtures` and `tests/selfcheck` are never in `Run.Path`.** |

Dispatch: expand `ci` to `@('clean','build','lint','test')` (plan 01-04 appends
`selfcheck`), run the requested tasks in that canonical order, wrap the whole
thing in try/catch, print `BUILD FAILED: <message>` plus the exception to stderr
and `exit 1`; on success print `BUILD SUCCEEDED (<tasks>) on PowerShell
<version>` and `exit 0`. The explicit `exit` codes are what CI and the clean-clone
check in plan 01-04 depend on — do not rely on `$LASTEXITCODE` defaults.

`tests/contract` will not exist until plan 01-02; make `Invoke-HDTTest` tolerate a
missing path by filtering `Run.Path` to directories that exist, and throw if
**none** exist.

### PSScriptAnalyzerSettings.psd1

```powershell
@{
    IncludeDefaultRules = $true
    Severity            = @('Error','Warning')
    ExcludeRules        = @()
    Rules = @{
        PSUseCompatibleSyntax = @{
            Enable         = $true
            TargetVersions = @('5.1','7.0')
        }
    }
}
```
Verified: this configuration flags `$a ?? 'x'` as a `PSUseCompatibleSyntax`
**Error**, giving a second, independent layer of 5.1 enforcement alongside the
AST contract test built in plan 01-02.

### README.md

Replace `Readme.txt` (delete it, `git rm`). Short and factual: what HDT is, the
PS 5.1 constraint, repo layout (from DESIGN §15.2), how to run
`./build.ps1 -Task test` / `lint` / `ci` under both engines, the
`Verb-HDTNoun` rule with a one-line pointer to the contract test, and links to
`docs/DESIGN.md` and `docs/ROADMAP.md`. No badges (there is no remote yet).

### .gitignore

Add `/out/` is already covered; append `out/` result artefacts only if missing.
Verify `out/testResults/*.xml` is ignored — if not, add it.
  </action>
  <verify>
```
pwsh        -NoProfile -File ./build.ps1 -Task test ; echo "pwsh test exit=$LASTEXITCODE"
powershell  -NoProfile -File ./build.ps1 -Task test ; echo "5.1 test exit=$LASTEXITCODE"
pwsh        -NoProfile -File ./build.ps1 -Task lint ; echo "pwsh lint exit=$LASTEXITCODE"
pwsh        -NoProfile -File ./build.ps1 -Task ci   ; echo "pwsh ci   exit=$LASTEXITCODE"
```
All four print exit=0. Then prove the failure path: temporarily add
`It 'x' { $true | Should -BeFalse }` to a unit test, re-run `build.ps1 -Task test`,
confirm exit=1, and **revert it**.
  </verify>
  <done>
`./build.ps1 -Task test` exits 0 under pwsh 7 and powershell.exe 5.1; `lint`
exits 0 under pwsh 7 with zero diagnostics; a failing test produces exit 1;
`out/testResults/*.xml` is written; `README.md` replaces `Readme.txt`.
  </done>
</task>

</tasks>

<verification>
1. `pwsh -NoProfile -File ./build.ps1 -Task ci` -> exit 0.
2. `powershell.exe -NoProfile -File ./build.ps1 -Task test` -> exit 0.
3. `Import-Module ./src/Hephaestus/Hephaestus.psd1 -Force; Get-Command -Module Hephaestus`
   returns exactly `Get-HDTModuleVersion`.
4. `Get-Command Test-HDTSchemaVersion -ErrorAction SilentlyContinue` returns nothing.
5. `git log --oneline` shows test commits preceding their implementation commits.
6. `Select-String -Path (Get-ChildItem src,tests,build.ps1 -Recurse -Include *.ps1,*.psm1) -Pattern '\?\?|\?\.' ` finds no PS7-only operators (a crude pre-check; the real one lands in 01-02).
</verification>

<success_criteria>
- `./build.ps1 -Task test` is green under pwsh 7 **and** Windows PowerShell 5.1.
- `./build.ps1 -Task lint` is green under pwsh 7 and fails with an actionable
  message (not a crash) under 5.1 where PSScriptAnalyzer is unavailable.
- The module exports exactly its manifest's `FunctionsToExport`; private
  functions are reachable only via `InModuleScope`.
- Every function created in this plan matches `Verb-HDTNoun` with an approved verb.
- No PS7-only syntax anywhere in `src/`, `tests/`, or `build.ps1`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-harness/01-01-SUMMARY.md`.
Record: the generated module GUID, the exact Pester import incantation used, the
`Get-HDTSourceFile` include/exclude rules, and the `build.ps1` task list — plans
01-02 through 01-04 all build directly on those four things.
</output>
