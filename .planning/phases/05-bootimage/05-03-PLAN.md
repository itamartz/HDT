---
phase: 05-bootimage
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/Hephaestus/Payload/Start-HDTDeployment.ps1
  - src/Hephaestus/Public/Get-HDTBootstrapConfiguration.ps1
  - src/Hephaestus/Public/New-HDTContentProvider.ps1
  - src/Hephaestus/Public/Set-HDTLogPath.ps1
  - src/Hephaestus/Public/Invoke-HDTTaskSequence.ps1
  - src/Hephaestus/Public/Steps/Invoke-HDTDiskPartitionStep.ps1
  - src/Hephaestus/Hephaestus.psd1
  - tests/fixtures/bootstrap/valid-smb.json
  - tests/fixtures/bootstrap/valid-local.json
  - tests/fixtures/bootstrap/valid-prompt.json
  - tests/fixtures/bootstrap/invalid-unknown-provider.json
  - tests/fixtures/bootstrap/invalid-missing-deployroot.json
  - tests/fixtures/bootstrap/unparseable-truncated.json
  - tests/fixtures/README.md
  - tests/unit/Get-HDTBootstrapConfiguration.Tests.ps1
  - tests/unit/New-HDTContentProvider.Tests.ps1
  - tests/unit/Set-HDTLogPath.Tests.ps1
  - tests/unit/Invoke-HDTTaskSequence.LogRelocation.Tests.ps1
  - tests/unit/StartHDTDeploymentPayload.Tests.ps1
  - tests/unit/Imaging.EndToEnd.Tests.ps1
  - docs/DESIGN.md
autonomous: true

must_haves:
  truths:
    - "A file in the boot image tells the engine where the content is and which provider reaches it, and the engine builds that provider without guessing"
    - "Start-HDTDeployment.ps1 connects, gathers, resolves, runs the sequence once, and copies the logs back on success AND on failure - and does no deployment work of its own, proven by parsing it"
    - "The WinPE log moves to the target volume the moment one is formatted, so the WinPE-phase log survives the reboot into Windows"
    - "The seq counter and the log history survive the move; nothing already written is lost"
    - "A machine that cannot reach its content, or whose sequence fails, still ends with logs somewhere a human can read them"
  artifacts:
    - path: "src/Hephaestus/Payload/Start-HDTDeployment.ps1"
      provides: "THE WinPE ENTRY POINT - what startnet.cmd runs"
      min_lines: 250
    - path: "src/Hephaestus/Public/Set-HDTLogPath.ps1"
      provides: "DESIGN 4.4.1's relocation, mirroring the live log to the target volume"
      min_lines: 120
    - path: "src/Hephaestus/Public/Get-HDTBootstrapConfiguration.ps1"
      provides: "The read half of the boot image's bootstrap.json"
      min_lines: 100
    - path: "tests/unit/StartHDTDeploymentPayload.Tests.ps1"
      provides: "The AST proof that the entry point does nothing a step would do"
      min_lines: 200
  key_links:
    - from: "src/Hephaestus/Payload/Start-HDTDeployment.ps1"
      to: "Invoke-HDTTaskSequence"
      via: "exactly one call, with -State and -LogDestination"
      pattern: "Invoke-HDTTaskSequence"
    - from: "src/Hephaestus/Public/Invoke-HDTTaskSequence.ps1"
      to: "Set-HDTLogPath"
      via: "after a step publishes HDTOSVolume in the WinPE phase, the log relocates once"
      pattern: "Set-HDTLogPath"
    - from: "src/Hephaestus/Payload/Start-HDTDeployment.ps1"
      to: "Copy-HDTLog"
      via: "a copy-back that runs after the catch, so a run that died before the loop still lands its logs"
      pattern: "Copy-HDTLog"
---

<objective>
The WinPE entry point itself — `Start-HDTDeployment.ps1` — and the one piece of
logging DESIGN 4.4.1 specifies that phase 04 deliberately deferred: `_HDTLogPath`
relocating to the target volume as soon as one is formatted.

Purpose: phase 04 proved the engine deploys a machine, but a human typed the line
that started it and the WinPE-phase log died with the RAM disk at the reboot.
Those are the same defect wearing two hats — nobody is at the keyboard, and
nobody can see what happened. This plan writes the thing startnet.cmd will run
(05-04 puts it in the image) and makes the log survive the machine it was written
on.

Output: `Start-HDTDeployment.ps1`, `Get-HDTBootstrapConfiguration`,
`New-HDTContentProvider`, `Set-HDTLogPath`, the relocation wired into the loop,
and an AST test file that holds the entry point to the same discipline
`Start-HDTLabDeployment.ps1` is already held to.
</objective>

<execution_context>
@C:/Users/Itamartz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Itamartz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/SPIKES.md
@docs/DESIGN.md
@docs/ROADMAP.md
@tests/helpers/README.md
@.planning/phases/05-bootimage/05-01-SUMMARY.md
@.planning/phases/05-bootimage/05-02-SUMMARY.md
@.planning/phases/04-imaging/04-04-SUMMARY.md

**Read these two files completely before writing a line.** They are the shape
this plan copies and the standard it is held to:

- `tests/e2e/payload/Start-HDTLabDeployment.ps1` — phase 04's stand-in, which
  says in its own header that it is "THE PHASE-04 STAND-IN FOR PHASE 05's
  Start-HDTDeployment.ps1".
- `tests/unit/StartHDTLabDeploymentPayload.Tests.ps1` — the AST test that proves
  a launcher does no deployment work. `tests/unit/StartHDTResumePayload.Tests.ps1`
  is the same technique for the full-OS payload.

Binding sections: **DESIGN 4.4.1** (`_HDTLogPath` follows the deployment;
copy-back on failure too), **DESIGN 6.3** (the embedded credential is what the
machine connects with), **DESIGN 4.3** (state document, resume), **DESIGN 11**
(the technician UI in WinPE — *not* in scope here; this plan writes a silent
entry point and says so).

Reference implementation: `C:\HDTLab\reference\PSD\Scripts\PSDStart.ps1` — the
closest prior art for exactly this file. Mine it for the *mechanism*: how it
waits for the network, how it retries a share connection, what it does with
`X:\` versus `C:\` state, what it logs when it cannot go on. Strip every MDT
dependency (rule 4) and do not copy its shape — it calls hardware directly and
this file may not.
</context>

<verified_facts>
Probed on this machine and recorded in SPIKES. Do not re-derive.

- **WinPE's RAM disk is `X:`, and that is the one letter that IS guaranteed**
  (SPIKES S9.1). Everything else is not: WinPE assigned the *content disk* `C:`
  in the phase-04 run. So `X:\HDT\…` may be written literally in this file, and
  no other drive letter may be.
- **`powershell-yaml` 0.4.12 imports inside WinPE from a staged copy** on a plain
  data disk, `net47` flavour against `WinPE-NetFx` (SPIKES S9.1). The engine
  cannot read one YAML document without it, so it is imported first and its
  version is logged.
- **WinPE has no `NetTCPIP` and no `NetAdapter`** (DESIGN 5.1). Waiting for an
  address means `Win32_NetworkAdapterConfiguration` through `ICimProvider`, or
  `netsh`. `Get-HDTMachineFact` already returns `HDTIPAddress` and
  `HDTDefaultGateway` from exactly that source (SPIKES S9.2) — reuse it rather
  than writing a second gatherer.
- **WinPE's clock is wrong** (DESIGN 4.4.2, `clockUnsynced`), measured at 14:31
  while the host was at 00:31. Nothing here may compute a timeout from wall-clock
  timestamps written by a previous leg.
- The engine reported **100 s** for the five-step WinPE leg, of which the apply
  was ~95–134 s, inside a 273 s wall-clock boot-to-shutdown (SPIKES S9.12).
- Phase 04 deferred the relocation deliberately and said so in
  `Invoke-HDTDiskPartitionStep.ps1`'s help: *"IT DOES NOT MOVE THE LOG… it
  belongs with phase 05's Start-HDTDeployment."* That comment is now wrong and
  this plan rewrites it.
</verified_facts>

<engine_semantics>
**Read this before writing a test.**

### `bootstrap.json` — what the boot image carries, and the only file that tells the engine where it is

Written into the image at `X:\HDT\bootstrap.json` by `Update-HDTBootImage`
(05-04). **This plan defines and reads it; 05-04 writes it**, and 05-04 carries a
test that the file it writes is accepted by this reader.

```json
{
  "schemaVersion": 1,
  "workspaceId":   "HDT-LAB",
  "provider":      "Smb",
  "deployRoot":    "\\\\server\\HdtShare",
  "sequenceId":    "",
  "credential":    { "username": "CONTOSO\\svc-hdt-deploy", "protected": "…" },
  "promptForCredential": false,
  "logLevel":      "Info",
  "buildId":       "…",
  "builtUtc":      "…"
}
```

```
Get-HDTBootstrapConfiguration -Path <string> [-FileSystem <object>]
```
reads through `IFileSystem`, parses with `ConvertFrom-Json`, validates, applies
defaults, returns a `[pscustomobject]`. Rules, each a failing test:

- `provider` outside `Smb|Local` → `HDTConfigurationError` naming the file, the
  value and the two;
- `deployRoot` missing or empty → error;
- `provider: Smb` with a `deployRoot` that is not UNC → error naming both;
- `credential` absent **and** `promptForCredential` false **and**
  `provider: Smb` → error saying the image was built without a credential and
  naming `-PromptForCredential`;
- `promptForCredential: true` → the credential block may be absent and the
  result reports `PromptForCredential $true`;
- a truncated / unparseable file → `HDTConfigurationError` naming the file. **Not
  a raw `ConvertFrom-Json` exception**: this file is read on a machine with no
  operator, and its failure is the last thing anyone will see;
- `sequenceId` empty is legal — the sequence then comes from the rules, which is
  DESIGN 3's answer to "which task sequence does this machine get".
- **Under Windows PowerShell 5.1 `ConvertFrom-Json` does not enumerate a
  top-level array and returns `PSCustomObject` for objects** — assign first, wrap
  second (helpers README F12; it bit 04-04 twice).

### `New-HDTContentProvider` — the one place a provider name becomes a provider

```
New-HDTContentProvider -Provider <Smb|Local> -Root <string>
                       [-Credential <pscredential>] [-FileSystem] [-SmbService]
                       [-Journal] [-AllowAnonymous]
```
A two-branch factory and nothing else, so the payload does not carry a `switch`
and a future `Http` provider (DESIGN 6) lands in one file. Tests:
`It 'builds a Local provider for Local'`, `It 'builds an Smb provider for Smb'`,
`It 'passes the credential to the Smb provider'`,
`It 'refuses an unknown provider name'`,
`It 'names Http as not implemented in v1'` — DESIGN 6 says the interface exists
so a cloud transport can land later; a caller asking for it deserves that
sentence, not "unknown".

### `Set-HDTLogPath` — DESIGN 4.4.1's relocation

```
Set-HDTLogPath -Context <logContext> -TargetVolume <string>
               [-Variable <IDictionary>]  -> [string] the new path
```

What it does, in order:

1. Compute the destination with `Get-HDTLogPath -Phase WinPE -TargetVolume`.
   **Do not build it by hand** — that function already owns the answer.
2. If the destination equals the current `Context.LogPath`, return it and do
   nothing. Idempotent, because the loop may ask more than once.
3. Create the destination through `Context.FileSystem`.
4. **Copy everything already written**, preserving the tree — `HDT.log`,
   `HDT.jsonl`, `status.json`, `Steps\`, `Gather\`, `Native\`. This is a mirror,
   not a move: the RAM-disk copy stays, because a relocation that deleted the
   only log and then failed would be worse than no relocation.
5. Repoint the context: `LogPath`, `JsonlPath`, `MasterLogPath`. **`Seq` is not
   touched** — DESIGN 4.4.2's counter is monotonic across the whole run and a
   reset here would be exactly the ambiguity it exists to prevent.
6. When `-Variable` is supplied, set `_HDTLogPath` in it. The engine variable is
   read-only *to sequences and rules*; the engine itself is what sets it, which
   is what the leading underscore means (DESIGN 4.4.1).
7. Write one `message` record naming **both** paths, through the *already
   repointed* context, so the first line in the new file explains why the file
   exists.
8. **It never throws.** A target volume that cannot be written — full, not
   formatted after all, a letter that went away — produces a `Warning` record
   through the old context, leaves everything pointing at `X:`, and returns the
   old path. Losing the logs is not an acceptable price for moving the logs.

### Where the loop triggers it

In `Invoke-HDTTaskSequence`, after each step's result is recorded and its
variables are published, and **only** when all four hold:

- `Context.Phase -eq 'WinPE'`;
- `Context.Variable['HDTOSVolume']` is now non-empty;
- the current `Log.LogPath` is still the RAM-disk path
  (`Get-HDTLogPath -Phase WinPE` with no target);
- the step just run reported `Completed`.

Then call `Set-HDTLogPath -Context $log -TargetVolume $osVolume -Variable $Context.Variable`.
One decision point, in the one place that sees every step finish.

### `Start-HDTDeployment.ps1` — the entry point

`src/Hephaestus/Payload/Start-HDTDeployment.ps1`. `Payload\` is not loaded by the
module (the loader dot-sources `Private\` and `Public\` only), so it ships as a
script and is staged to `X:\HDT\`, exactly as `Start-HDTResume.ps1` is staged to
`C:\HDT\`.

```
param(
  [string] $BootstrapPath = 'X:\HDT\bootstrap.json',
  [string] $ModuleRoot    = 'X:\HDT\Modules',
  [string] $SequenceId,                 # overrides the bootstrap and the rules
  [string] $LogRoot,                    # default Get-HDTLogPath -Phase WinPE
  [int]    $NetworkTimeoutSecond = 120
)
```

Eleven things, in this order, and **nothing else**:

1. `Set-StrictMode -Version Latest`, `$ErrorActionPreference = 'Stop'`,
   `$InformationPreference = 'Continue'` (visible at the WinPE console without
   `Write-Host`, which the analyzer refuses).
2. Put `$ModuleRoot` on `PSModulePath`; import **`powershell-yaml` first**, then
   `Hephaestus`; log the version of each. The order is the dependency proof —
   `ConvertFrom-HDTYaml` reports `HDTDependencyError` without it and the engine
   cannot read one document.
3. Build the real adapters: `New-HDTFileSystem`, `New-HDTClock`,
   `New-HDTDiskService`, `New-HDTImageService`, `New-HDTRegistryService`,
   `New-HDTLsaService`, `New-HDTEnvironmentProvider`, `New-HDTCimProvider`,
   `New-HDTProcessService`, `New-HDTPowerService`, `New-HDTScriptInvoker`. **A
   fake never appears in this file.**
4. Build a log context at `$LogRoot` (default `X:\HDT\Logs`) **before** anything
   can fail, so step 5's failure is logged rather than printed and lost.
5. `Get-HDTBootstrapConfiguration`.
6. Wait for the network, but **only when `provider` is `Smb`**: poll
   `Get-HDTMachineFact`'s `HDTIPAddress` until it holds a non-APIPA IPv4 or
   `-NetworkTimeoutSecond` expires, logging each attempt. A `Local` provider does
   not wait, and that is the difference between a lab run starting in five
   seconds and starting in two minutes.
7. `New-HDTContentProvider` from the bootstrap; `Connect()`; log the root.
   On refusal — including the guest refusal from 05-02 — log at `Error` and go to
   step 11 with `Failed`.
8. `Get-HDTMachineFact` → `Get-HDTMachineOverride` (keyed on `HDTUUID`) →
   `Import-HDTRuleDocument` → `Resolve-HDTVariable`, with the sequence's own
   defaults, exactly as `Start-HDTLabDeployment.ps1` already does. Which sequence:
   `-SequenceId`, else `bootstrap.sequenceId`, else the resolved
   `HDTTaskSequenceId` variable; **none of the three set is a named failure**, not
   a guess.
9. `New-HDTRunState`, `New-HDTExecutionContext` with a catalog that carries the
   **Content** provider.
10. **One** call to `Invoke-HDTTaskSequence`, with `-State`, `-StatePath` and
    `-LogDestination` pointing at `<deployRoot>\Logs`.
11. `finally`: write `X:\HDT\RESULT.json` (UTF-8, no BOM, via
    `System.Text.UTF8Encoding` with `$false`), call `Copy-HDTLog` **again**
    unconditionally — the loop already does it when it got that far, and this is
    for the runs that did not — `Disconnect()` the provider, then act on the
    outcome:
    - `RebootPending` → `wpeutil reboot`
    - anything else → `wpeutil shutdown`
    Both after the `catch`, never inside the `try`.

**What this file must NOT do**, and the AST test enforces every line of it:
no Storage cmdlet, no DISM cmdlet, no `bcdboot`/`bcdedit`/`reagentc`/`diskpart`,
no `Invoke-HDT*Step`, no literal workspace folder name (`Get-HDTWorkspacePath`
owns those), no `New-HDTFake*`, no drive-letter scan other than `X:`, no
`Write-Host`, and **no technician UI** — DESIGN 11's progress window and wizard
are a later milestone and a silent entry point is the honest v1.

`RESULT.json` carries: `runId`, `status`, `failedStep`, `message`,
`computerName`, `sequenceId`, `provider`, `deployRoot`, `connected`,
`yamlLoaded`, `yamlVersion`, `engineVersion`, `psVersion`, `elapsedSecond`,
`logPath`, `logDestination`, and **`launchedBy`** — read from
`$env:HDT_LAUNCHED_BY`, which `startnet.cmd` sets (05-04) and a human typing the
command by hand does not. That single field is what 05-05 asserts to prove the
deployment started itself.
</engine_semantics>

<tasks>

<task type="auto">
  <name>Task 1: the bootstrap document and the provider factory</name>
  <files>
tests/fixtures/bootstrap/valid-smb.json
tests/fixtures/bootstrap/valid-local.json
tests/fixtures/bootstrap/valid-prompt.json
tests/fixtures/bootstrap/invalid-unknown-provider.json
tests/fixtures/bootstrap/invalid-missing-deployroot.json
tests/fixtures/bootstrap/unparseable-truncated.json
tests/fixtures/README.md
tests/unit/Get-HDTBootstrapConfiguration.Tests.ps1
tests/unit/New-HDTContentProvider.Tests.ps1
src/Hephaestus/Public/Get-HDTBootstrapConfiguration.ps1
src/Hephaestus/Public/New-HDTContentProvider.ps1
src/Hephaestus/Hephaestus.psd1
  </files>
  <action>
Fixtures follow `tests/fixtures/state/*.json`'s naming exactly (`valid-*`,
`invalid-*`, `unparseable-*`) and are listed in `tests/fixtures/README.md` as
**authored**, not captured, with a line saying 05-04 writes the real thing and
its test asserts this reader accepts it.

### RED — `tests/unit/Get-HDTBootstrapConfiguration.Tests.ps1`

One `It` per rule in `<engine_semantics>`, plus:
- `It 'reads through the injected filesystem'` — from the fake's journal
- `It 'applies Info as the default log level'`
- `It 'returns PromptForCredential false when the key is absent'`
- `It 'does not return the protected secret in a property that gets logged'` —
  the credential is exposed as a `pscredential` built on demand by a method, and
  a plain `ToString()` of the result must not contain the protected string
- `It 'accepts the fixture 05-04 will write'` — read
  `tests/fixtures/bootstrap/valid-smb.json` and assert every projected property

### RED — `tests/unit/New-HDTContentProvider.Tests.ps1`

The six `It`s named in `<engine_semantics>`, against fakes.

### GREEN

Both files, exported, with comment-based help. `Get-HDTBootstrapConfiguration`
wraps the parse in a `try` and rethrows `HDTConfigurationError` naming the file —
per DESIGN 13.1 and because nobody is watching when this fails.

Commits: `test(05-03): failing tests for the bootstrap document` /
`feat(05-03): the boot image's bootstrap document and the provider factory`.
  </action>
  <verify>
```
pwsh       -NoProfile -File ./build.ps1 -Task ci
powershell -NoProfile -File ./build.ps1 -Task test
```
Both exit 0. **Both**, because `ConvertFrom-Json` differs between the two engines
and this file parses JSON (helpers README F12).
  </verify>
  <done>
A boot image can say where its content is and how to reach it, in one validated
file, and a malformed one produces a sentence rather than a stack trace on a
machine with no operator.
  </done>
</task>

<task type="auto">
  <name>Task 2: _HDTLogPath relocation — the WinPE log survives the reboot</name>
  <files>
tests/unit/Set-HDTLogPath.Tests.ps1
tests/unit/Invoke-HDTTaskSequence.LogRelocation.Tests.ps1
tests/unit/Imaging.EndToEnd.Tests.ps1
src/Hephaestus/Public/Set-HDTLogPath.ps1
src/Hephaestus/Public/Invoke-HDTTaskSequence.ps1
src/Hephaestus/Public/Steps/Invoke-HDTDiskPartitionStep.ps1
src/Hephaestus/Hephaestus.psd1
docs/DESIGN.md
  </files>
  <action>
This is the deliverable phase 04 deferred on purpose and recorded in commit
`3979b26`. It is worth stating in the code why it matters:
**a deployment that dies in WinPE loses its log at the reboot, and dying in WinPE
is exactly when the log is wanted.**

### RED — `tests/unit/Set-HDTLogPath.Tests.ps1`

All against `New-HDTFakeFileSystem` and `New-HDTFakeClock`.

`Context 'the move'`
- `It 'returns the path Get-HDTLogPath computes for the target volume'`
- `It 'accepts W: and W:\ as the same volume'`
- `It 'creates the destination'`
- `It 'repoints JsonlPath and MasterLogPath'`
- `It 'sets _HDTLogPath in the variable dictionary when one is given'`
- `It 'does not touch Seq'` — DESIGN 4.4.2's monotonic counter
- `It 'is idempotent when the context is already there'` — no second copy, no
  second record
- `It 'writes a message record naming both paths'`
- `It 'writes that record into the NEW file'`

`Context 'what it carries across'`
- `It 'copies HDT.jsonl and HDT.log'`
- `It 'copies the Steps folder with its structure'` —
  `Steps\003-ApplyImage.dism.log` arrives at the same relative path
- `It 'copies Gather and Native'`
- `It 'leaves the RAM disk copy in place'` — it is a mirror; a move that failed
  halfway would have destroyed the only copy

`Context 'when it cannot'`
- `It 'never throws when the destination cannot be created'`
- `It 'keeps logging to the old path'` — the context is unchanged
- `It 'writes a warning naming the target volume and the reason'`
- `It 'returns the old path'`

### RED — `tests/unit/Invoke-HDTTaskSequence.LogRelocation.Tests.ps1`

Built on `New-HDTSequenceTestHarness`, the same way the reboot and resume suites
are:

- `It 'relocates after the step that publishes HDTOSVolume'`
- `It 'does not relocate before it'` — after step 1 the log is still on `X:`
- `It 'relocates exactly once across the leg'` — count the `message` records
- `It 'does not relocate in the FullOS phase'`
- `It 'does not relocate when HDTOSVolume is empty'`
- `It 'does not relocate after a step that failed'`
- `It 'keeps seq continuous across the relocation'` — the numbers in the new file
  continue the old file's, with no repeat and no gap. **This is the assertion
  that matters**: DESIGN 4.4.2 exists because timestamps skew, and a relocation
  that reset the counter would break the one ordering that is trustworthy
- `It 'writes every later record to the new path only'`
- `It 'copies back from the relocated path at the end'` — `Copy-HDTLog` is given
  the new `LogPath`, so `<share>\Logs\<name>-<runId>` holds the whole run

Extend `tests/unit/Imaging.EndToEnd.Tests.ps1` — the M3 benchmark — with one
assertion in its existing ordered-operation-list shape:
`It 'has moved the log to the target volume by the end of the WinPE leg'`.
The benchmark is the file that describes what HDT does to a machine; the log
moving is now part of that.

### GREEN

`Set-HDTLogPath.ps1` (public, exported), the trigger inside
`Invoke-HDTTaskSequence`, and **rewrite** the "IT DOES NOT MOVE THE LOG" block in
`Invoke-HDTDiskPartitionStep.ps1`'s help to say what happens now: the step
publishes `HDTOSVolume`, the loop relocates, and the step still does not do it
itself — because the step does not own the log context and a step that reached
into it would be the wrong shape.

Add one paragraph to `docs/DESIGN.md` 4.4.1 recording that the relocation is a
**mirror**, that it is triggered by the loop rather than by the partition step,
and that it never fails a run.

Commits: `test(05-03): failing tests for the log relocation` /
`feat(05-03): _HDTLogPath follows the deployment to the target volume` /
`docs(05-03): DESIGN 4.4.1 - the relocation is a mirror, and the loop owns it`.
  </action>
  <verify>
Both build commands exit 0. Then read the evidence rather than trusting the
count:

```powershell
Invoke-Pester ./tests/unit/Invoke-HDTTaskSequence.LogRelocation.Tests.ps1 -Output Detailed
Invoke-Pester ./tests/unit/Imaging.EndToEnd.Tests.ps1 -Output Detailed
```
Both green, and the relocation file's `seq` test names the exact numbers on
either side of the move in its failure message so a regression says what broke.
  </verify>
  <done>
DESIGN 4.4.1's table is true in code for all four rows. A WinPE deployment that
fails after partitioning leaves its log on a volume that still exists after the
reboot, and 05-05 reads it off the deployed VHDX to prove it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Start-HDTDeployment.ps1 — the WinPE entry point, and the AST proof it does nothing else</name>
  <files>
tests/unit/StartHDTDeploymentPayload.Tests.ps1
src/Hephaestus/Payload/Start-HDTDeployment.ps1
  </files>
  <action>
### RED FIRST — `tests/unit/StartHDTDeploymentPayload.Tests.ps1`

Write this **before** the payload, and watch it fail on a file that does not
exist. Copy the structure of `tests/unit/StartHDTLabDeploymentPayload.Tests.ps1`
wholesale — the `commandNamed` closure, the `codeOnly` token stream with comments
dropped (so a header sentence saying "this file calls no bcdboot" cannot fail a
raw text scan and teach the next author to delete the sentence), the forbidden
command and forbidden text lists.

`Describe 'Start-HDTDeployment.ps1'`
- `It 'exists at src/Hephaestus/Payload/Start-HDTDeployment.ps1'`
- `It 'parses with no error'`
- `It 'passes the PowerShell 5.1 compatibility scanner'` —
  `Get-HDTScriptCompatibilityViolation`; it runs in WinPE where there is no pwsh
- `It 'defines no unprefixed function'`
- `It 'sets StrictMode and ErrorActionPreference'`

`Context 'it does no deployment work itself'`
- `It 'names no fake'`
- `It 'names no Storage or DISM cmdlet'`
- `It 'names no native boot tool'` — `bcdboot`, `bcdedit`, `reagentc`,
  `diskpart`, `dism.exe`, scanned over the comment-free token stream
- `It 'calls Invoke-HDTTaskSequence exactly once'`
- `It 'invokes no step function directly'`
- `It 'writes no unattend and stages no image'`
- `It 'contains no technician UI'` — no `Show-`, no `Add-Type` naming
  `PresentationFramework` or `System.Windows.Forms`. DESIGN 11 is a later
  milestone and this file must not quietly become it

`Context 'the dependency proof'`
- `It 'imports powershell-yaml before it imports Hephaestus'` — compared by
  extent offset, as the lab launcher's test does
- `It 'logs the version of each module it loaded'`
- `It 'puts the module root on PSModulePath'`
- `It 'assumes only X: and no other drive letter'` — scan the comment-free token
  stream for `[C-WYZ]:\\` and assert nothing matches. SPIKES S9.1: WinPE gave the
  content disk `C:` and the RAM disk `X:`; `X:` is the only guarantee

`Context 'what it builds and what it hands the loop'`
- `It 'builds the real service adapters'` — the eleven `New-HDT*` factories
- `It 'builds the content provider through New-HDTContentProvider'`
- `It 'reads the bootstrap through Get-HDTBootstrapConfiguration'`
- `It 'gathers facts and resolves variables exactly once each'`
- `It 'looks for a per-machine override'` — `Get-HDTMachineOverride`, DESIGN 3.1
  source 2, which 04-04 proved is what makes a machine an exception
- `It 'builds every workspace path through Get-HDTWorkspacePath'` — and the text
  contains no `'TaskSequences'`, `'Logs'` or `'Control'` literal
- `It 'passes -State, -StatePath and -LogDestination to the loop'`
- `It 'passes the content provider into the service catalog'`

`Context 'it always leaves evidence and always ends'`
- `It 'writes RESULT.json'`
- `It 'writes it as UTF-8 without a BOM'` — `UTF8Encoding`
- `It 'records launchedBy from HDT_LAUNCHED_BY'` — the field 05-05 asserts
- `It 'calls Copy-HDTLog outside the try'` — by extent offset, after the largest
  `TryStatementAst`'s end. A run that died before the loop never reached the
  loop's own copy-back, and that run is precisely the one whose log is wanted
- `It 'disconnects the content provider'`
- `It 'ends the machine with wpeutil'`
- `It 'reboots on RebootPending and shuts down otherwise'` — both strings present
- `It 'ends the machine even when the run threw'` — the `wpeutil` call's offset is
  greater than the outermost try's end offset, the same assertion the lab
  launcher's test makes

### GREEN — the payload

Write it against the eleven-step order in `<engine_semantics>`. Its header
comment must carry, in the voice the other two payloads use:

- what it is (THE WinPE ENTRY POINT — what `startnet.cmd` runs);
- that it contains no deployment logic and that
  `tests/unit/StartHDTDeploymentPayload.Tests.ps1` proves it by parsing;
- that `X:` is the only letter it may assume, and why (SPIKES S9.1);
- that `powershell-yaml` is imported first, and why;
- that it ends the machine in every path, because a VM left at a prompt tells
  nobody anything;
- that DESIGN 11's technician UI is deliberately absent.

Commits: `test(05-03): failing tests for the WinPE entry point` /
`feat(05-03): Start-HDTDeployment.ps1, the boot image's entry point`.
  </action>
  <verify>
Both build commands exit 0. Then run the file the way it will actually be run,
on the developer's machine, against a **Local** provider and a scratch workspace,
with `-SequenceId` pointing at a NoOp sequence:

```powershell
$env:HDT_LAUNCHED_BY = 'manual-smoke'
./src/Hephaestus/Payload/Start-HDTDeployment.ps1 `
    -BootstrapPath C:\HDTLab\scratch\bootstrap-smoke.json `
    -ModuleRoot ./src -LogRoot C:\HDTLab\scratch\smoke-logs -WhatIf:$false
```
It must **not** be run with a real imaging sequence on this machine. Use a
`NoOp`-only sequence, confirm `RESULT.json` appears with
`launchedBy manual-smoke`, and confirm the `wpeutil` call fails harmlessly on a
full OS (there is no `wpeutil.exe` outside WinPE) — which is itself worth a line
in the summary, because it means the smoke run cannot power the developer's
machine off.
  </verify>
  <done>
The file `startnet.cmd` will run exists, does nothing a step would do, is proven
by parsing rather than by hope, and leaves a machine with logs and a power state
in every path including the ones where it failed before it started.
  </done>
</task>

</tasks>

<verification>
1. `pwsh -NoProfile -File ./build.ps1 -Task ci` → exit 0.
2. `powershell.exe -NoProfile -File ./build.ps1 -Task test` → exit 0.
3. `git log --oneline` shows a `test(05-03)` before every `feat(05-03)`.
4. `Select-String -Path src/Hephaestus/Payload/Start-HDTDeployment.ps1 -Pattern '\b[C-WYZ]:\\'`
   returns nothing — `X:` is the only literal drive letter in the file.
5. `Select-String -Path src/Hephaestus/Public/Steps/Invoke-HDTDiskPartitionStep.ps1 -Pattern 'IT DOES NOT MOVE THE LOG'`
   returns nothing; the block now describes the relocation that exists.
6. `Get-Command -Module Hephaestus | Where-Object Name -like '*LogPath*'` lists
   both `Get-HDTLogPath` and `Set-HDTLogPath`.
7. The relocation suite's `seq` continuity test is green under **both** engines.
</verification>

<success_criteria>
- One call to `Invoke-HDTTaskSequence`, asserted by AST, in the file that a boot
  image runs with nobody watching.
- Copy-back happens on the path where the run died before the loop, not only on
  the path where it succeeded.
- `_HDTLogPath` follows the deployment, the mirror keeps the history, and `seq`
  is continuous across the move.
- The entry point assumes exactly one drive letter, and it is the one WinPE
  guarantees.
</success_criteria>

<output>
After completion, create `.planning/phases/05-bootimage/05-03-SUMMARY.md`.

Record, because 05-04 writes the file this plan reads and 05-05 asserts on what
this plan produces: the exact `bootstrap.json` shape and every validation rule;
the `RESULT.json` field list, including `launchedBy` and where it comes from; the
`Set-HDTLogPath` semantics and the four conditions the loop tests before calling
it; the eleven-step order of the entry point; and the exact list of things the
AST test forbids, so 05-04 does not stage something that violates it.
</output>
