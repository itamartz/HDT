---
phase: 08-windows-update
plan: 08-01-04
type: execute
wave: 4
depends_on: ["08-01-03"]
files_modified:
  - src/Hephaestus/Public/Steps/Get-HDTWindowsUpdateStepTemplate.ps1
  - src/Hephaestus/Public/Steps/Get-HDTWindowsUpdateStepDescription.ps1
  - src/Hephaestus/Private/Get-HDTConsoleStepCatalog.ps1
  - src/Hephaestus/Private/Get-HDTStepPropertyDefinition.ps1
  - src/Hephaestus/Private/Get-HDTStepPropertyChoice.ps1
  - src/Hephaestus/Private/Assert-HDTSequenceDocument.ps1
  - schemas/sequence.schema.json
  - src/Hephaestus/Hephaestus.psd1
  - docs/command-categories.psd1
  - docs/command-reference.html
  - docs/DESIGN.md
  - docs/ROADMAP.md
  - samples/workspace/TaskSequences/STD-CLIENT/sequence.yaml
  - samples/workspace/TaskSequences/STD-SERVER/sequence.yaml
  - src/Hephaestus/Templates/client.yaml
  - tests/contract/StepContract.Tests.ps1
  - tests/unit/ConsoleStepCatalog.Tests.ps1
  - tests/unit/StepPropertyDefinition.Tests.ps1
  - tests/unit/WindowsUpdateStepSurface.Tests.ps1
  - tests/e2e/WindowsUpdate.E2E.Tests.ps1
  - tests/e2e/payload/UPD-WSUS/sequence.yaml
  - tests/e2e/README.md
  - .planning/SPIKES.md
autonomous: false

user_setup:
  - service: "HDT-WSUS-01 (lab WSUS server)"
    why: "The E2E patches a target VM against it, and a NotReady update is offered to nobody"
    dashboard_config:
      - task: "Confirm KB5121003 and KB5120710 both report State 'Ready' before the E2E runs"
        location: "HDT-WSUS-01, Get-WsusUpdate, or C:\\WSUS\\WsusContent"

must_haves:
  truths:
    - "A new WindowsUpdate step can be created from the console's Add menu and the YAML it writes loads"
    - "Every surface that must know about a step type knows about this one, proven by a test written against the set"
    - "The sample sequences run the step twice, once before applications and once after"
    - "The shipped client template carries both steps, disabled, the way MDT's Client.xml does"
    - "A VM deploys through HDT, is patched against HDT-WSUS-01, and comes back running 26100.9168"
  artifacts:
    - path: "src/Hephaestus/Public/Steps/Get-HDTWindowsUpdateStepTemplate.ps1"
      provides: "What a new WindowsUpdate step looks like on disk"
    - path: "tests/unit/WindowsUpdateStepSurface.Tests.ps1"
      provides: "The set-based proof that every surface learned about the type"
    - path: "tests/e2e/WindowsUpdate.E2E.Tests.ps1"
      provides: "The milestone exit, on a machine"
  key_links:
    - from: "src/Hephaestus/Private/Get-HDTConsoleStepCatalog.ps1"
      to: "WindowsUpdate"
      via: "the $known table"
      pattern: "'WindowsUpdate'"
    - from: "tests/e2e/WindowsUpdate.E2E.Tests.ps1"
      to: "HDT-WSUS-01"
      via: "the sequence's HDTWSUSServer variable"
      pattern: "HDTWSUSServer"
---

<objective>
Rule 8: a thing is not added until every surface that must know about it does —
and it is proven with a test written against the SET, not against the one you just
added. Then the milestone exit, on a machine.

Purpose: `Invoke-HDTWindowsUpdateStep` exists after plan 08-01-03 and the registry
discovers it by name, but a step type with no template cannot be CREATED, does not
appear in the console's Add menu, has no properties page, is invisible to the
document validator, and is in no sample. CLAUDE.md lists six surfaces that have
each silently shipped a half-feature in this repository already. This plan closes
all of them and then proves the whole thing on a VM.

Output: template, description, console catalog and properties, validator, schema,
docs, both samples running the step twice, a set-based surface test, and the E2E.
</objective>

<execution_context>
@C:/Users/Itamartz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Itamartz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/DESIGN.md
@CLAUDE.md
@.planning/phases/08-windows-update/08-01-03-SUMMARY.md
@src/Hephaestus/Public/Steps/Get-HDTInstallApplicationsStepTemplate.ps1
@src/Hephaestus/Public/Steps/Get-HDTInstallApplicationsStepDescription.ps1
@src/Hephaestus/Private/Get-HDTConsoleStepCatalog.ps1
@src/Hephaestus/Private/Get-HDTStepPropertyDefinition.ps1
@samples/workspace/TaskSequences/STD-CLIENT/sequence.yaml
@tests/e2e/UnattendedDeployment.E2E.Tests.ps1
@tests/contract/StepContract.Tests.ps1
</context>

<constraints>
- **TDD** for every function and every surface assertion.
- **Windows PowerShell 5.1 syntax.** `Verb-HDTNoun`, uppercase HDT.
- **Every VM this plan touches is `HDT-*` and created by the harness.**
  `HDT-WSUS-01` and `HDT-WDS-01` are read, never modified, never stopped, never
  removed — plan 08-01-01's stamp makes the last of those structurally impossible.
- **The target VM goes on `HDT External`**, Generation 2, files under
  `C:\HDTLab\vms\`, 4 GB. It needs DHCP and it needs to reach both the share and
  WSUS; `HDT Lab` is isolated, gets no lease and cannot reach either (SPIKES S6).
- **The host's own address is a lease.** Read it before building a boot image;
  never write the octet into a test, a plan or a commit message.
- **`HDTAdminPassword` is in `C:\HDTLab\Share\rules.yaml`.** Read it, pass the
  object, print neither. It never goes into a test, a fixture, a doc or a commit
  message.
- **The gate is Windows PowerShell 5.1** and the pwsh 7 pass is off. Never run
  both shell suites at once — they collide over real DISM artefacts.
- **Do not edit the tree during a gate run** (MEMORY): a mid-run edit locks the
  bundle and fabricates ~130 cascade failures.
- **This plan is the widest in the phase — twenty-three files across three
  tasks — and Task 3 is a 45-minute machine run whose evidence is read out of
  logs.** Execute Task 3 in a FRESH context: finish Tasks 1 and 2, run the gate,
  commit, and start the E2E clean. Reading a deployment's logs on a context
  already carrying twenty registration edits is how a real finding gets skimmed.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: The template, the description, and the set-based surface test</name>
  <files>
tests/unit/WindowsUpdateStepSurface.Tests.ps1
src/Hephaestus/Public/Steps/Get-HDTWindowsUpdateStepTemplate.ps1
src/Hephaestus/Public/Steps/Get-HDTWindowsUpdateStepDescription.ps1
src/Hephaestus/Private/Get-HDTConsoleStepCatalog.ps1
src/Hephaestus/Private/Get-HDTStepPropertyDefinition.ps1
src/Hephaestus/Private/Get-HDTStepPropertyChoice.ps1
src/Hephaestus/Private/Assert-HDTSequenceDocument.ps1
schemas/sequence.schema.json
src/Hephaestus/Hephaestus.psd1
tests/contract/StepContract.Tests.ps1
tests/unit/ConsoleStepCatalog.Tests.ps1
tests/unit/StepPropertyDefinition.Tests.ps1
  </files>
  <action>
**RED FIRST.** `tests/unit/WindowsUpdateStepSurface.Tests.ps1` — and note the
shape of these: the ones that matter walk the SET.

```
Describe 'Every step type reaches every surface it has to'
  It 'has a template for every type the console offers to add'         # walks Get-HDTStepType
  It 'has a description for every type this repository ships'
  It 'has a console catalog entry for every creatable type'            # existing assertion, must now cover WindowsUpdate
  It 'has a properties row set or a dedicated page for every creatable type'
  It 'exports every Invoke-HDT*Step function it ships from the manifest'
  It 'names every type it ships in the step contract''s expected list'

Describe 'Get-HDTWindowsUpdateStepTemplate'
  It 'is exported by the module'
  It 'writes a step whose type is WindowsUpdate'
  It 'writes runIn: FullOS, because the agent does not exist in WinPE'
  It 'reads the server from %HDTWSUSServer%'
  It 'writes continueOnError: true, the recommended default'
  It 'writes maxPasses: 3'
  It 'writes a categories list'
  It 'produces YAML that loads'
  It 'produces YAML that Import-HDTSequenceDocument accepts'
  It 'takes a name and uses it'

Describe 'Get-HDTWindowsUpdateStepDescription'
  It 'names the server the step will use'
  It 'reports the variable rather than guessing at what it will hold'
  It 'says Windows Update when the step names no server'
  It 'mentions the pass limit'
```

The first `Describe` is the one CLAUDE.md is about. Where an existing test already
walks the set — `tests/unit/ConsoleStepCatalog.Tests.ps1` line ~134 builds its list
from `Get-HDTStepType`, `tests/unit/StepPropertyDefinition.Tests.ps1` line ~392
walks every templated type, `tests/contract/StepTemplate.Contract.Tests.ps1` parses
every template — **do not duplicate it here**. Point at it in a comment and assert
only what is not covered. A second copy of a set assertion is a second thing to
keep in step.

**Then the surfaces.**

1. **`Get-HDTWindowsUpdateStepTemplate`** — model on
   `Get-HDTInstallApplicationsStepTemplate`. The lines:

```
- name: Windows Update
  type: WindowsUpdate
  server: '%HDTWSUSServer%'
  categories: [SecurityUpdates, CriticalUpdates, UpdateRollups]
  maxPasses: 3
  continueOnError: true
  runIn: FullOS
```

   **Single quotes on the token.** `StepTemplate.Contract.Tests.ps1` exists
   because the `ApplyDrivers` template shipped a double-quoted scalar containing
   a backslash and no YAML parser would open the result.

   The help explains four choices, in the voice the other templates use:
   `server` arrives as the variable so a rule or a wizard answer works without
   editing the sequence again, and an empty `HDTWSUSServer` means Windows Update
   — which is DESIGN 10.1's documented behaviour, not a fallback. `runIn: FullOS`
   is written out because WUA does not exist in WinPE and plan 08-01-03's import
   refuses the step in a WinPE group. `continueOnError: true` is DESIGN 10.1's
   recommended default and the reason is worth one line: Windows Update is the
   least predictable thing in a sequence. `maxPasses: 3` is written out so an
   author can SEE that the step loops — a template that omitted it would teach
   the single-pass mistake by silence.

2. **`Get-HDTWindowsUpdateStepDescription`** — one line for the log and the
   progress display. `'WindowsUpdate: %HDTWSUSServer%, up to 3 passes'`;
   `'WindowsUpdate: Windows Update, up to 3 passes'` with no server. A token is
   reported as the variable, never resolved — the description is built from the
   step as authored, with no context (the `InstallApplications` description says
   this at length; match it).

3. **`Get-HDTConsoleStepCatalog`** — add to `$known`:
   `'WindowsUpdate' = @{ Text = 'Install Updates Offline'... }` — **no.** MDT's
   own step for this is **"Install Updates Offline"** and that is the OFFLINE one,
   which is HDT's `ApplyUpdates` (DESIGN 7.5). This step is MDT's
   **"Windows Update (Post-Application Installation)"** / "(Pre-Application
   Installation)" pair. Use `Text = 'Windows Update'`, `Category = 'General'`,
   `Order = 10` (after `Tattoo`). Comment it: the catalog names steps as MDT does
   where MDT has one, and MDT ships this step twice under two names purely to
   express placement; HDT expresses placement with the step's position in the
   tree, so it is offered once and the samples show the convention.
   **Check what `ApplyUpdates` is already called in this table** before choosing,
   so the two do not collide — DESIGN separates the names twice for this reason.

4. **`Get-HDTStepPropertyDefinition`** — a row set for `WindowsUpdate`, in the
   order the template writes the keys (the file says that is the rule):
   `server` Text (default: 'Windows Update'), `categories` List,
   `exclude` List, `maxPasses` Text (default '3'). Hints are ONE OR TWO LINES —
   CLAUDE.md has written that rule down twice. `maxPasses`: "How many times to
   search and install before stopping. One pass leaves a machine half-patched."
   `exclude`: "Titles with wildcards, or KB numbers. `*Preview*`, `KB5001234`."
   The reasoning goes in the comment above the row, not under the control.

5. **`Get-HDTStepPropertyChoice`** — `categories` is a closed set:
   `SecurityUpdates`, `CriticalUpdates`, `UpdateRollups`, `Updates`,
   `DefinitionUpdates`, `Drivers`, `FeaturePacks`, `ServicePacks`, `Tools`.
   Add it here and let the definition table read it — the file says spelling a
   closed set twice is two lists to drift apart, and the way that goes wrong is a
   drop-down offering a value the step rejects at the machine.

6. **`Assert-HDTSequenceDocument`** — read it before deciding what to change. It
   validates the COMMON keys and refuses unknown keys at ROOT and GROUP level; the
   step node's own extra keys pass through, so `server`/`categories`/`exclude`/
   `maxPasses` need nothing. **Verify that by test rather than by reading** — add
   an `It 'accepts a WindowsUpdate step with every key DESIGN 10.1 documents'` to
   this task's file. If it turns out a key IS refused, list it there; CLAUDE.md
   names this surface precisely because a new key is invisible until it is.

7. **`schemas/sequence.schema.json`** — same: the `step` definition has no
   `additionalProperties: false`, so the keys validate today. Confirm with
   `tests/contract/SequenceSchema.Contract.Tests.ps1` and add an assertion that
   the sample sequences, which will now carry the step, still validate. Change the
   schema only if a test says to.

8. **Manifest** — `Get-HDTWindowsUpdateStepTemplate` and
   `Get-HDTWindowsUpdateStepDescription` into `FunctionsToExport`.
   `Invoke-HDTWindowsUpdateStep` went in during plan 08-01-03.

9. **`tests/contract/StepContract.Tests.ps1`** — add `'WindowsUpdate'` to
   `$script:HDTExpectedStepType`. That list is hand-written on purpose and the file
   explains why at its foot; read that note before touching it.
  </action>
  <verify>
```bash
powershell.exe -NoProfile -Command "Invoke-Pester tests/unit/WindowsUpdateStepSurface.Tests.ps1, tests/unit/ConsoleStepCatalog.Tests.ps1, tests/unit/StepPropertyDefinition.Tests.ps1 -Output Detailed"
powershell.exe -NoProfile -Command "Invoke-Pester tests/contract/StepContract.Tests.ps1, tests/contract/StepTemplate.Contract.Tests.ps1, tests/contract/SequenceSchema.Contract.Tests.ps1, tests/contract/ConsoleSurface.Contract.Tests.ps1 -Output Detailed"
```

Then run the module:

```bash
powershell.exe -NoProfile -Command "Import-Module ./src/Hephaestus/Hephaestus.psd1 -Force; Get-HDTStepType -Name WindowsUpdate | Format-List; Get-HDTWindowsUpdateStepTemplate"
```

`CanAdd` must now be `$true`.
  </verify>
  <done>
`Get-HDTStepType -Name WindowsUpdate` reports `CanAdd $true` with all three
commands. The console catalog offers "Windows Update" under General. Every
set-walking test that previously reported the type as missing is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: The samples run it twice, and the docs stop calling it deferred</name>
  <files>
samples/workspace/TaskSequences/STD-CLIENT/sequence.yaml
samples/workspace/TaskSequences/STD-SERVER/sequence.yaml
src/Hephaestus/Templates/client.yaml
docs/DESIGN.md
docs/ROADMAP.md
docs/command-categories.psd1
docs/command-reference.html
tests/unit/WindowsUpdateStepSurface.Tests.ps1
  </files>
  <action>
**RED FIRST** — add to `tests/unit/WindowsUpdateStepSurface.Tests.ps1`:

```
Describe 'The sample sequences show the convention'
  It 'runs Windows Update twice in the client sample'
  It 'runs it once before the applications step and once after'
  It 'runs it twice in the server sample'
  It 'imports the client sample without error'
  It 'imports the server sample without error'
  It 'puts both in a FullOS group'
```

**Then the samples.** DESIGN 10.1: "Placement: conventionally run twice — once
before applications, once after — since app installs can pull in updatable
components. The sample sequences do this."

`STD-CLIENT/sequence.yaml` already carries the step commented out at lines
~101-102, with a note at line 6 recording which unbuilt steps remain. Uncomment
it, add the second instance, and update that note — it is a comment about the
state of the repository and it is now wrong.

**Splice, never re-serialise** (MEMORY, and CLAUDE.md rule 8): comments die at
parse time and these files are half commentary. Edit the YAML text.

The two instances, in the State Restore group:

```yaml
      # DESIGN 10.1: TWICE, AND THE ORDER IS THE POINT. The first pass patches
      # the base image so an installer meets a current machine; the second
      # catches the updatable components an install pulled in - .NET, the VC
      # runtimes, a driver an application shipped.
      - name: Windows Update (Pre-Application)
        type: WindowsUpdate
        server: '%HDTWSUSServer%'
        maxPasses: 3
        continueOnError: true

      - name: Install Applications
        type: InstallApplications
        selection: '%HDTApplications%'

      - name: Windows Update (Post-Application)
        type: WindowsUpdate
        server: '%HDTWSUSServer%'
        maxPasses: 3
        continueOnError: true
```

The names are MDT's own for its two instances of this step — an administrator
arriving from Workbench is looking for those words.

`STD-SERVER/sequence.yaml`: the same two, around its `InstallApplications` at
line ~85. It has an `InstallRoles` at line ~73 and a `Restart` at ~82; put the
pre-application pass AFTER that restart, and comment why — a role install that
asked for a reboot must land before the update agent runs, or the first search
happens on a machine with a pending restart and returns stale results.

**AND THE SHIPPED TEMPLATE, WHICH IS SURFACE NUMBER ONE IN CLAUDE.md's OWN
TABLE.** `src/Hephaestus/Templates/client.yaml` is what `New-HDTWorkspace` seeds
every new share from — "the templates are the product, the share is a copy of
them" — and it carries an `Install Applications` step in State Restore with no
Windows Update anywhere near it. The samples are not a substitute for it: a
technician who runs `New-HDTWorkspace` gets `client.yaml`, not `STD-CLIENT`.
That table's first row is this exact failure: "`client.yaml` authored partitions
with no drive letter and could not partition a disk. Templates were parsed and
schema-checked; nothing ever *planned* one."

**Both steps go in, and both go in `disabled: true` — because that is what MDT
does, checked rather than remembered.** MDT's own
`C:\Program Files\Microsoft Deployment Toolkit\Templates\Client.xml` on this host
ships `<step name="Windows Update (Pre-Application Installation)" disable="true">`
and the same for `(Post-Application Installation)`. MEMORY: "HDT is a homage to
MDT" — when MDT and a fresh idea disagree about behaviour, build MDT's. And the
reason is good on its own: a template that patched by default would send every
new workspace's first deployment to Windows Update over the internet, unasked, on
a bench where somebody was expecting the image's own patch level.

So: the two steps, named as MDT names them, bracketing `Install Applications`,
each with `disabled: true`, `server: '%HDTWSUSServer%'` and a comment saying that
removing the `disabled` line and setting `HDTWSUSServer` in `rules.yaml` or on
the wizard is the whole of turning it on. `disabled` is a common key
`Assert-HDTSequenceDocument` already validates, so this needs nothing new.

Add `It 'ships both Windows Update steps disabled, as MDT's Client.xml does'` and
`It 'still imports the shipped client template'` to
`tests/unit/WindowsUpdateStepSurface.Tests.ps1`.

**THEN BRING THIS LAB'S SHARE UP TO IT.** CLAUDE.md, rule 8's corollary: a
change to `Templates\` that `C:\HDTLab\Share` does not have is a change nobody
here can test, because the next deployment runs the SHARE's copy. An existing
share is never written over — those files are somebody's edits — so **splice**
the two steps into the share's own client sequence, never replace the file, and
say in the summary which file was spliced. The 08-01-04 E2E builds its own
workspace from the repository and is unaffected either way; this is about the
share the next person deploys from.

**Then the docs.**

`docs/DESIGN.md` §10.1 — remove the `· **DEFERRED TO v2**` from the heading and
the "v2, not cut" block quote. **Do not delete the history**; replace it with:

> **Deferred out of v1 on 2026-08-16 and built on 2026-09-06**, once a real WSUS
> server existed in the lab. The section was kept in full through the deferral
> precisely so that building it started from a written plan rather than a memory,
> and the step type being additive meant it added files rather than changing them
> — which is what happened.

Also fix the two forward references that now say the wrong thing: line ~57
(§1's deferred list: "No `WindowsUpdate` step. A machine HDT builds leaves…"),
line ~468 ("`WindowsUpdate` is the online full-OS step against WSUS, and is
deferred to v2"), and line ~3246 in §15's open question 4.

`docs/ROADMAP.md` — the "Deferred out of M6 to v2" block at lines ~637-654. Same
treatment: it is now built, the "what deferring it costs" paragraph is history
and should be marked as such rather than deleted, and the **Tests first** list for
it needs writing down as what was actually done. Add the exit criterion this
phase closes, with the date and the build number, in the style of M7's
"✅ **MET on…**" entries.

`docs/command-categories.psd1` — the three new commands, in the same group as the
other step commands. Then **regenerate** `docs/command-reference.html` with
`tools/New-HDTCommandReference.ps1`; do not hand-edit it.
  </action>
  <verify>
```bash
powershell.exe -NoProfile -Command "Invoke-Pester tests/unit/WindowsUpdateStepSurface.Tests.ps1 -Output Detailed"
powershell.exe -NoProfile -Command "Invoke-Pester tests/contract/CommandReference.Contract.Tests.ps1, tests/contract/ShippedDefault.Contract.Tests.ps1, tests/contract/ShippedDocumentSchema.Contract.Tests.ps1, tests/contract/SequenceTemplatePlan.Contract.Tests.ps1, tests/contract/ExampleQuality.Contract.Tests.ps1 -Output Detailed"
```

`SequenceTemplatePlan` is the one that matters here and it is why it is on the
list: it PLANS the shipped templates rather than parsing them, because
`client.yaml` once shipped a partition table that could not partition a disk and
"ten thousand tests and a full end-to-end VM deployment" missed it. `client.yaml`
is edited in this task, so it is planned again before anything else runs.

```bash
```

Then the whole gate, backgrounded, ticking every 2 minutes:

```bash
./build.ps1 ci
```
  </verify>
  <done>
Both samples import, each carries two `WindowsUpdate` steps bracketing
`InstallApplications`, and their comments survived the edit. No document still
calls the step deferred. `./build.ps1 ci` green under Windows PowerShell 5.1.
  </done>
</task>

<task type="auto">
  <name>Task 3: The E2E — a VM deploys, patches against HDT-WSUS-01, and comes back on 26100.9168</name>
  <files>
tests/e2e/WindowsUpdate.E2E.Tests.ps1
tests/e2e/payload/UPD-WSUS/sequence.yaml
tests/e2e/README.md
.planning/SPIKES.md
  </files>
  <action>
The milestone exit, on a machine.

**BEFORE ANYTHING ELSE, four checks. Each of them has a failure mode that looks
exactly like a broken step, and each costs 45 minutes to discover the slow way.**

1. **Is the big CU ready?** The server is deliberately staged: of 599 synced
   updates exactly TWO are approved — **KB5121003**, the 2026-08 cumulative for
   Windows 11 24H2 x64 (build **26100.9168**), and **KB5120710**, the matching
   .NET 3.5/4.8.1 cumulative. The other 597 are DECLINED. That is what makes a
   client coming back on 26100.9168 prove the STEP worked rather than proving
   Microsoft's catalogue works.

   **KB5121003 is 14 GB and was still downloading into `C:\WSUS\WsusContent` when
   this phase was planned.** A `NotReady` update is offered to NOBODY, and the
   step would correctly report nothing applicable — which is indistinguishable
   from a step that does not work. Check `State` is `Ready` on both approved
   updates before starting. Reach the server the way `.planning/PROJECT.md`
   describes; its local Administrator password is the share's `HDTAdminPassword`
   in `C:\HDTLab\Share\rules.yaml`, because HDT deployed it (`run-20260902-203748`).
   **Read it, pass the object, print it nowhere.**

   If it is not ready: **stop and report it**, do not proceed and do not
   improvise a different update. Write the finding into the summary.

2. **The memory budget.** `Get-HDTLabMemoryUse` must report `AssignedByte 0` with
   `HDT-WDS-01` and `HDT-WSUS-01` in `Ignored`. If it does not, plan 08-01-01 did
   not land and the VM will be refused.

3. **The host's address.** Read it — it is a lease, it has moved on its own
   before, and it gets baked into the boot image:
   `Get-NetIPAddress -AddressFamily IPv4 -InterfaceAlias 'vEthernet (HDT External)'`.
   Never write the octet into the test, the plan, or a commit message.

4. **Does the target's network resolve `HDT-WSUS-01` by NAME?** The sequence
   points the client at `http://HDT-WSUS-01:8530` deliberately — the target gets
   DHCP and DNS from the real LAN on `HDT External`, and an address in the YAML
   would be a second lease written down. But that only works if the LAN's DNS
   carries the WSUS server's DHCP registration, and `HDT-WSUS-01` is a workgroup
   machine. **Resolve it from this host before the run** — `Resolve-DnsName
   'HDT-WSUS-01'` — and confirm the reply is on `192.168.1.0/24`. A name that
   does not resolve makes `SetServer` write a policy pointing at nothing, the
   search return nothing applicable, and the step report exactly what a working
   step reports on a fully-patched machine.

   If it does not resolve: **stop and report it.** Do not substitute an address
   into the sequence and do not invent a hosts-file entry on the target — either
   would make the E2E prove something other than what a real deployment does,
   and the address is a lease this repository does not write down.

**The sequence**, `tests/e2e/payload/UPD-WSUS/sequence.yaml`, seeded onto the
share by the test on every run (the pattern the other E2E files use — "ONE PLACE
OF TRUTH AND THE SHARE HOLDS A COPY"; it is this file's own sequence, so
refreshing it destroys nobody's edits, and the share's OTHER sequences are never
written by it). Model it on the existing `client` template plus:

```yaml
    variables:
      HDTWSUSServer: 'http://HDT-WSUS-01:8530'
```

Port **8530** (8531 is the TLS one). Use the NAME, not an address — the target
gets DHCP and DNS from the real LAN on the `HDT External` switch, and an address
here would be a second lease written down.

One `WindowsUpdate` step in the State Restore group, `maxPasses: 3`,
`continueOnError: false` — **the E2E wants a failure to fail**, which is the one
place the recommended default is wrong. Say so in a comment so nobody
"corrects" it.

**The test file**, `tests/e2e/WindowsUpdate.E2E.Tests.ps1`, following
`UnattendedDeployment.E2E.Tests.ps1`'s structure exactly:

- Opt-in: skip unless `$env:HDT_RUN_WINDOWS_UPDATE -eq '1'`. It costs 45+ minutes
  and a 14 GB download over the lab network. **The guard is a `BeforeDiscovery`
  variable read by `-Skip:` on the `Describe`, and it is StrictMode-safe** —
  `tests/contract/SlowSuiteSkip.Contract.Tests.ps1` judges every slow suite in
  the repository for exactly this and will fail the file otherwise. SPIKES S9.15:
  "without StrictMode it evaluated to `$null`, and `if (-not $null)` is TRUE — so
  the expensive body ran on a machine that was supposed to be skipping it." A new
  slow file that gets this wrong deploys a VM during an ordinary `./build.ps1 ci`.
- `Assert-HDTLabMemoryBudget` before starting anything.
- The lab-safety `AfterAll` that runs on failure too: every VM **not** created by
  this harness enumerated before and asserted identical after, with the count
  asserted separately so an empty host reads as "there was nothing to protect"
  rather than "nothing was harmed". **Enumerate, never name** — that assertion
  rotted once by comparing two empty arrays and passing.
- Teardown through `Remove-HDTLabVirtualMachine` unless `$env:HDT_KEEP_LAB_VM -eq '1'`.

`Describe`/`It`:

```
Describe 'the engine ran the update step'          -Tag 'E2E'
  It 'ran the WindowsUpdate step'                                  # state.json / the master log
  It 'pointed the client at the lab WSUS server'                   # log names the server and where the value came from
  It 'found the two approved updates and no others'                # 2, not 599 - the server staging is the control
  It 'installed KB5121003'
  It 'installed KB5120710'
  It 'ran more than one pass'                                      # the whole point of DESIGN 10.1
  It 'restarted between passes and resumed at the next pass'       # -Reenter, not pass one again
  It 'stopped when a pass found nothing applicable'                # not because maxPasses was hit
  It 'logged the KB, the title, the classification and the elapsed time of each update'

Describe 'the machine it patched'                  -Tag 'E2E'
  It 'boots into Windows'
  It 'reports build 26100.9168'                    # THE EXIT CRITERION
  It 'reports KB5121003 among its installed updates'

Describe 'the lab is unharmed'                     -Tag 'E2E'
  It 'left every VM this harness did not create exactly as it found it'
  It 'left HDT-WSUS-01 running'
  It 'left HDT-WDS-01 running'
  It 'removed the VM it created'
```

The build number comes from the guest. Reach it the way the other E2E files reach
a deployed machine — read the run's `state.json` and the master log off the
share's `Logs\`, and take the version from the machine itself
(`Get-ItemProperty 'HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion'` →
`CurrentBuild` + `UBR` = `26100.9168`) captured by the sequence's final
`PowerShell` step into a file the share can read. Do NOT plan on a live remote
session to the target — it is a workgroup VM on a switch this laptop reaches by
DHCP, and every previous E2E in this repository took its evidence off the share
for that reason.

**Then write down what it cost.** Add a `.planning/SPIKES.md` entry —
verified-by-execution findings only, in that file's numbered style: what the real
WUA did that the fake did not predict, how long a 14 GB CU took over the lab
network, whether the servicing stack update forced an extra pass, and anything
about `ServerSelection`/`wuauserv` that had to be true before a search returned
anything. That file exists so the next agent does not re-hit a trap; a phase that
runs a real update agent for the first time and records nothing has wasted the
run. Add the E2E to `tests/e2e/README.md`'s list with its opt-in variable and its
cost.
  </action>
  <verify>
Pre-flight, then the run:

```bash
powershell.exe -NoProfile -Command "Import-Module ./tests/helpers/HDTTestTools/HDTTestTools.psd1 -Force; Get-HDTLabMemoryUse | Format-List"
powershell.exe -NoProfile -Command "Hyper-V\Get-VM -Name 'HDT-*' | Select-Object Name, State, MemoryAssigned, Notes"
```

Then, elevated, backgrounded, ticking every 2 minutes with a real date-time:

```bash
HDT_RUN_WINDOWS_UPDATE=1 powershell.exe -NoProfile -Command "Invoke-Pester tests/e2e/WindowsUpdate.E2E.Tests.ps1 -Output Detailed"
```
  </verify>
  <done>
Every `It` green. The target reports `26100.9168`. `HDT-WSUS-01` and `HDT-WDS-01`
are still running with `Notes` still empty. Every VM not created by the harness is
as it was found. `.planning/SPIKES.md` carries the new entry.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The `WindowsUpdate` step, end to end: the service, the step, every authoring
surface, both samples, and an E2E that deploys a VM through HDT and patches it
against `HDT-WSUS-01`.
  </what-built>
  <how-to-verify>
1. **The console offers it.** Open the console, right-click a task sequence's
   step tree, Add → General. "Windows Update" is on the menu. Add one, open its
   properties, and confirm the four rows (server, categories, exclude, maxPasses)
   with one-or-two-line hints. **A screenshot comes with this checkpoint,
   unasked** — CLAUDE.md: change the UI, show the UI. Render it offscreen with an
   STA probe and `RenderTargetBitmap`; do not `ShowDialog` on the desktop.
2. **The E2E result.** The build number the target came back on, and the pass
   count from the step's log.
3. **The lab.** `Hyper-V\Get-VM | Select Name, State, MemoryAssigned, Notes` —
   `HDT-WSUS-01` and `HDT-WDS-01` running, unchanged, unstamped.
4. **The gate.** `./build.ps1 ci` green under Windows PowerShell 5.1.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what is wrong</resume-signal>
</task>

</tasks>

<verification>
Restated as checkable assertions — this is the milestone exit:

1. **A VM deploys through HDT.** The run's `state.json` reports the sequence
   completed and the machine booted into Windows.
2. **The `WindowsUpdate` step patched it against `HDT-WSUS-01`.** The step's log
   names the server, reports two applicable updates out of the server's approved
   set, and records `KB5121003` and `KB5120710` installed with per-update timing.
3. **It comes back running 26100.9168.**
4. **More than one pass ran**, with a reboot between them, and the step stopped
   because a pass found nothing applicable — not because `maxPasses` was hit.
5. **The WSUS and WDS servers and the target were the only VMs running**, and
   every VM not created by the harness is exactly as it was found.
6. `./build.ps1 ci` green under Windows PowerShell 5.1, analyzer in its own
   process.
</verification>

<success_criteria>
- [ ] Template, description, console catalog, properties rows, property choices,
      validator, schema, manifest, command categories and the regenerated command
      reference all know the type — each proven by a test written against the SET.
- [ ] Both sample sequences run the step twice, bracketing `InstallApplications`,
      and both still import.
- [ ] `src/Hephaestus/Templates/client.yaml` — the shipped template every new
      workspace is seeded from — carries both steps `disabled: true`, as MDT's
      `Client.xml` ships them, and `C:\HDTLab\Share` has been spliced to match.
- [ ] No document still calls the step deferred, and the deferral history is
      recorded rather than deleted.
- [ ] The E2E is written, is opt-in, and passes.
- [ ] `.planning/SPIKES.md` records what the real Windows Update Agent did.
- [ ] The console screenshot was sent unasked.
</success_criteria>

<output>
After completion, create
`.planning/phases/08-windows-update/08-01-04-SUMMARY.md` and
`.planning/phases/08-windows-update/08-01-VERIFICATION.md`.

The verification states the exit criteria as met-or-not with the evidence for
each, and states plainly anything that was NOT proven on a machine — the way M6's
verification says outright that `JoinDomain`'s logic is tested and its wire is not.
</output>
